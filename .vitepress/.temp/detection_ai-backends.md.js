import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Object detection & AI backends","description":"","frontmatter":{"title":"Object detection & AI backends"},"headers":[],"relativePath":"detection/ai-backends.md","filePath":"detection/ai-backends.md","lastUpdated":1785045033000}');
const _sfc_main = { name: "detection/ai-backends.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><div style="${ssrRenderStyle({ "display": "none" })}" hidden="true" aria-hidden="true" data-nosnippet>Are you an LLM? You can read better optimized documentation at /detection/ai-backends.md for this page in Markdown format</div><h1 id="object-detection-ai-backends" tabindex="-1">Object detection &amp; AI backends <a class="header-anchor" href="#object-detection-ai-backends" aria-label="Permalink to &quot;Object detection &amp; AI backends&quot;">​</a></h1><p>Object detection finds <strong>people, vehicles, and animals</strong> in the frame, and is the basis for faces, license plates, and semantic search. It runs on an <strong>AI backend</strong> you choose to match your hardware.</p><h2 id="choosing-a-backend" tabindex="-1">Choosing a backend <a class="header-anchor" href="#choosing-a-backend" aria-label="Permalink to &quot;Choosing a backend&quot;">​</a></h2><p>camera.ui ships six AI backends as plugins. Pick the one for your hardware and enable it per camera in <a href="/cameras/settings">Settings → Plugins</a> (see <a href="/sensors/setup">Set up sensors</a>):</p><ul><li><strong>CoreML.</strong> For Apple Silicon Macs; uses the GPU and Neural Engine.</li><li><strong>OpenVINO.</strong> For Intel CPUs and GPUs.</li><li><strong>ONNX.</strong> Cross-platform, on CPU or a supported GPU.</li><li><strong>NCNN.</strong> A lightweight backend for supported hardware. (No semantic search.)</li><li><strong>Coral.</strong> For a Coral Edge TPU, with a CPU fallback. Linux only.</li><li><strong>Hailo.</strong> For a Hailo-8 / Hailo-8L accelerator. Linux only.</li></ul><p>CoreML, OpenVINO, ONNX, and NCNN provide object, face, and license-plate detection. CoreML, OpenVINO, and ONNX also provide CLIP for <a href="/detection/semantic-search">semantic search</a>. Coral and Hailo are edge-AI accelerator backends that provide object detection only, and only for <strong>person, vehicle, and animal</strong><sup class="footnote-ref"><a href="#fn1" id="fnref1">[1]</a></sup> (no faces, license plates, or CLIP).</p><h2 id="models-and-confidence" tabindex="-1">Models and confidence <a class="header-anchor" href="#models-and-confidence" aria-label="Permalink to &quot;Models and confidence&quot;">​</a></h2><p>In a backend&#39;s plugin settings you choose a <strong>model</strong> for each task (object, face, license plate, CLIP) and a <strong>confidence threshold</strong>, which is how sure the AI must be before it reports a detection. Larger models are more accurate but heavier; higher confidence means fewer false alarms but more missed detections. camera.ui downloads the models it needs automatically.</p><p>There is also a per-camera <strong>Object Confidence</strong> in <a href="/cameras/settings">Settings → Detection</a>, between 0.3 and 1. Raise it if a camera reports objects that aren&#39;t there, lower it if it misses real ones.</p><p>Below it is <strong>Ignore stationary objects</strong>, on by default. Objects that stay put across events, like a car parked in the driveway, stop triggering new object events until they move again. Turn it off if you want every event to report the objects it sees, even the ones that never move.</p><h2 id="object-assist" tabindex="-1">Object assist <a class="header-anchor" href="#object-assist" aria-label="Permalink to &quot;Object assist&quot;">​</a></h2><p>Some cameras detect objects on their own but report only that they saw something, not where in the frame it is. Many Reolink models work this way. That leaves face and license-plate recognition nothing to zoom in on, so they rarely find anything.</p><p>Under the camera&#39;s <strong>Plugins</strong> tab, pick an <strong>Object Assist</strong> plugin below <strong>Object</strong>. It is a second detector that locates the person or vehicle in the frame, so face and plate recognition get a proper crop to work on. See <a href="/sensors/setup">Set up sensors</a>.</p><h2 id="what-you-get" tabindex="-1">What you get <a class="header-anchor" href="#what-you-get" aria-label="Permalink to &quot;What you get&quot;">​</a></h2><p>Detected objects appear on the live view as boxes, drive <a href="/notifications/">notifications</a>, and become events you can <a href="/recording/browsing">browse</a> and filter by type. They also feed <a href="/detection/faces">face recognition</a>, <a href="/detection/license-plates">license plates</a>, and <a href="/detection/semantic-search">semantic search</a>. A <strong>heatmap</strong> overlay on the player shows where objects were detected most often over a time window.</p><hr class="footnotes-sep"><section class="footnotes"><ol class="footnotes-list"><li id="fn1" class="footnote-item"><p>Coral and Hailo run a fixed object model that reports three classes. The other backends detect the full set of object types described above. <a href="#fnref1" class="footnote-backref">↩︎</a></p></li></ol></section></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("detection/ai-backends.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const aiBackends = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  aiBackends as default
};
