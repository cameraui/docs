import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Hardware acceleration","description":"","frontmatter":{"title":"Hardware acceleration"},"headers":[],"relativePath":"install/hardware-acceleration.md","filePath":"install/hardware-acceleration.md","lastUpdated":1784589260000}');
const _sfc_main = { name: "install/hardware-acceleration.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><div style="${ssrRenderStyle({ "display": "none" })}" hidden="true" aria-hidden="true" data-nosnippet>Are you an LLM? You can read better optimized documentation at /install/hardware-acceleration.md for this page in Markdown format</div><h1 id="hardware-acceleration" tabindex="-1">Hardware acceleration <a class="header-anchor" href="#hardware-acceleration" aria-label="Permalink to &quot;Hardware acceleration&quot;">​</a></h1><p>camera.ui accelerates two things: the <strong>video pipeline</strong> (decoding camera streams for detection, live view and snapshots, and encoding for streaming) and <strong>AI inference</strong> (object detection and everything built on it).</p><p><strong>Video acceleration is automatic on every platform.</strong> camera.ui probes the hardware at runtime, picks the best available backend and falls back to software if there is none, so there is nothing to configure. AI accelerators are automatic too once the hardware is reachable; on Docker that means passing the device through once (covered below).</p><h2 id="what-runs-where" tabindex="-1">What runs where <a class="header-anchor" href="#what-runs-where" aria-label="Permalink to &quot;What runs where&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Platform</th><th>Video pipeline (automatic)</th><th>AI backends</th></tr></thead><tbody><tr><td><strong>macOS</strong> (desktop app, Intel &amp; Apple Silicon)</td><td>VideoToolbox (Apple GPU + media engine)</td><td>CoreML (Apple GPU + <strong>Neural Engine</strong>), ONNX</td></tr><tr><td><strong>Windows</strong> (desktop app, x64)</td><td>NVIDIA (CUDA/NVENC), Intel (QSV), and any GPU via Direct3D 11/12</td><td>OpenVINO (Intel GPU/NPU), ONNX (NVIDIA CUDA)</td></tr><tr><td><strong>Linux x64</strong> (Docker, bare-metal, Proxmox)</td><td>NVIDIA (CUDA/NVENC), Intel (VA-API/QSV), AMD (VA-API), plus Vulkan/OpenCL</td><td>OpenVINO (Intel GPU/NPU), ONNX (CUDA/TensorRT), NCNN (Vulkan), Coral, Hailo</td></tr><tr><td><strong>Linux arm64</strong> (Rockchip)</td><td>Rockchip (RKMPP)</td><td>Coral, Hailo, NCNN (CPU/NEON)</td></tr></tbody></table><p>The table shows what camera.ui <strong>can</strong> use. The matching GPU driver still has to be installed on the host. camera.ui probes every backend at startup and only picks ones that actually work; anything whose driver is missing is skipped silently, never an error.</p><p>On the <strong>desktop app</strong> there is nothing to set up: macOS ships video acceleration as part of the OS, and on Windows the graphics driver comes with the system. Hardware is detected automatically. Each detection plugin logs what every model runs on (<code>Loaded model: …</code>) and shows it under <strong>Active Hardware</strong> in its plugin settings.</p><p>The rest of this page is for <strong>Linux and Docker</strong>, where hardware has to cross the container boundary. That always takes the same three steps:</p><ol><li><strong>Host driver.</strong> The kernel-side driver must be installed on the host (the only part a Docker image can never ship).</li><li><strong>Device passthrough.</strong> The device node goes into the <code>devices:</code> section of your compose file.</li><li><strong>Verify.</strong> The container tells you what actually arrived.</li></ol><p>GPUs are matched by picking the right <a href="/install/docker#hardware-acceleration">image flavor</a>. AI accelerators work with <strong>any</strong> flavor: the Coral runtime is baked into every image, and the Hailo runtime ships with its plugin. The one exception is the Intel NPU, whose user-space driver is bundled in the <strong>intel flavor</strong> only.</p><h2 id="check-your-host" tabindex="-1">Check your host <a class="header-anchor" href="#check-your-host" aria-label="Permalink to &quot;Check your host&quot;">​</a></h2><p>The <a href="https://github.com/cameraui/docker" target="_blank" rel="noreferrer"><code>cameraui/docker</code></a> repository ships a host helper that diagnoses your machine and installs the drivers that need more than a package manager:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">curl</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -fsSLO</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> https://raw.githubusercontent.com/cameraui/docker/main/scripts/host/cameraui-host.sh</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">sudo</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> bash</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> cameraui-host.sh</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> check</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">     # read-only: drivers, docker runtime, device nodes</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">sudo</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> bash</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> cameraui-host.sh</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> nvidia</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">    # install the NVIDIA Container Toolkit</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">sudo</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> bash</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> cameraui-host.sh</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> hailo</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">     # build + install the Hailo PCIe kernel driver</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">sudo</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> bash</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> cameraui-host.sh</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> coral</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">     # install the gasket/apex driver (PCIe/M.2 Coral)</span></span></code></pre></div><p><code>check</code> prints a ✓/✗ line for every accelerator class with the fix for anything missing. Run it first. It tells you which of the sections below apply to your machine.</p><h2 id="verify-from-the-container" tabindex="-1">Verify from the container <a class="header-anchor" href="#verify-from-the-container" aria-label="Permalink to &quot;Verify from the container&quot;">​</a></h2><p>On every start the container logs which device nodes it can actually see:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>[setup] accelerator devices: /dev/dri ✓ · /dev/kfd ✗ · /dev/accel ✗ · /dev/apex_0 ✓ · /dev/hailo0 ✗ · /dev/bus/usb ✗ · nvidia ✗</span></span></code></pre></div><p>If a device shows <code>✗</code> here but exists on the host, it is missing from your compose <code>devices:</code> list. The detection plugins additionally log what they found (<code>Available devices: …</code>) and what each model actually runs on (<code>Loaded model: … (AUTO -&gt; CPU)</code>), and show it under <strong>Active Hardware</strong> in their plugin settings.</p><h2 id="quick-reference" tabindex="-1">Quick reference <a class="header-anchor" href="#quick-reference" aria-label="Permalink to &quot;Quick reference&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Hardware</th><th>Accelerates</th><th>Host driver</th><th>Pass to the container</th></tr></thead><tbody><tr><td>Intel / AMD GPU</td><td>Video (VA-API/QSV) + OpenCL/Vulkan inference</td><td>in the kernel (i915/amdgpu)</td><td><code>/dev/dri</code></td></tr><tr><td>NVIDIA GPU</td><td>Video (NVENC/NVDEC) + CUDA</td><td>NVIDIA driver + Container Toolkit</td><td>via the toolkit runtime</td></tr><tr><td>Coral Edge TPU (PCIe/M.2)</td><td>Object detection</td><td>gasket/apex (<code>coral</code> command)</td><td><code>/dev/apex_0</code></td></tr><tr><td>Coral Edge TPU (USB)</td><td>Object detection</td><td>none</td><td><code>/dev/bus/usb</code></td></tr><tr><td>Hailo-8 / Hailo-8L</td><td>Object detection</td><td>hailo_pci + firmware (<code>hailo</code> command)</td><td><code>/dev/hailo0</code></td></tr><tr><td>Intel NPU (Core Ultra)</td><td>OpenVINO inference</td><td><code>intel_vpu</code> kernel module + firmware</td><td><code>/dev/accel</code> (intel flavor)</td></tr><tr><td>AMD ROCm</td><td>Compute</td><td>ROCm stack</td><td><code>/dev/kfd</code> + <code>/dev/dri</code></td></tr></tbody></table><h2 id="intel-amd-gpu" tabindex="-1">Intel / AMD GPU <a class="header-anchor" href="#intel-amd-gpu" aria-label="Permalink to &quot;Intel / AMD GPU&quot;">​</a></h2><p>The driver is part of the Linux kernel, so there is nothing to install on the host. Use the <code>intel</code> or <code>amd</code> flavor and pass <code>/dev/dri</code>:</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">    devices</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">      - </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">/dev/dri:/dev/dri</span></span></code></pre></div><div class="tip custom-block"><p class="custom-block-title">Older Intel iGPUs (Gen8 to Gen11, e.g. UHD 630)</p><p>Intel&#39;s current OpenCL runtime only covers Gen12 and newer. The <code>intel</code> flavor therefore bundles Intel&#39;s legacy runtime alongside it, so <strong>OpenVINO and OpenCL inference also work on Gen8 to Gen11 iGPUs</strong> (Broadwell through Comet Lake). Only chips older than Gen8 (Haswell and earlier) have no OpenCL runtime at all: there inference falls back to CPU, and a Coral or Hailo is the way to fast detection. Video decoding is fully accelerated on all of them either way.</p></div><h2 id="nvidia-gpu" tabindex="-1">NVIDIA GPU <a class="header-anchor" href="#nvidia-gpu" aria-label="Permalink to &quot;NVIDIA GPU&quot;">​</a></h2><p>Two host-side pieces: the NVIDIA driver (e.g. <code>sudo ubuntu-drivers install</code>) and the Container Toolkit, which injects the driver libraries into containers. <code>sudo bash cameraui-host.sh nvidia</code> installs the toolkit and registers it with Docker. Then use the <code>nvidia</code> flavor override from the <a href="/install/docker#hardware-acceleration">Docker page</a>. Setting <code>NVIDIA_DRIVER_CAPABILITIES=all</code> also enables the OpenCL and Vulkan paths the detection plugins can use.</p><h2 id="coral-edge-tpu" tabindex="-1">Coral Edge TPU <a class="header-anchor" href="#coral-edge-tpu" aria-label="Permalink to &quot;Coral Edge TPU&quot;">​</a></h2><p>The runtime library (<code>libedgetpu</code>) is already in every image flavor. Only the host side differs by form factor:</p><ul><li><p><strong>PCIe / M.2.</strong> Needs the gasket/apex kernel driver. <code>sudo bash cameraui-host.sh coral</code> installs it (a maintained community build, since Google&#39;s original package no longer compiles on kernels ≥ 6.13). Then pass the device:</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">    devices</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">      - </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">/dev/apex_0:/dev/apex_0</span></span></code></pre></div></li><li><p><strong>USB.</strong> No driver needed, just pass the USB bus:</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">    devices</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">      - </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">/dev/bus/usb:/dev/bus/usb</span></span></code></pre></div></li></ul><p>Without a reachable TPU the Coral plugin runs on CPU and says so in its log. Check its <code>Available devices:</code> line if detection seems slow.</p><h2 id="hailo-8-hailo-8l" tabindex="-1">Hailo-8 / Hailo-8L <a class="header-anchor" href="#hailo-8-hailo-8l" aria-label="Permalink to &quot;Hailo-8 / Hailo-8L&quot;">​</a></h2><p>The kernel driver is built from source on the host: <code>sudo bash cameraui-host.sh hailo</code> installs build dependencies, compiles the driver, installs the firmware and udev rules. <strong>Reboot afterwards</strong> so the firmware loads, then pass the device:</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">    devices</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">      - </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">/dev/hailo0:/dev/hailo0</span></span></code></pre></div><p>The userspace runtime ships inside the Hailo plugin. Nothing else to install.</p><h2 id="intel-npu-core-ultra" tabindex="-1">Intel NPU (Core Ultra) <a class="header-anchor" href="#intel-npu-core-ultra" aria-label="Permalink to &quot;Intel NPU (Core Ultra)&quot;">​</a></h2><p>The <strong>intel flavor</strong> bundles the NPU user-space driver (Level Zero + the Intel NPU UMD and compiler); other flavors do not. On the host you need the <code>intel_vpu</code> kernel module (upstream since Linux 6.8) and its firmware; if <code>/dev/accel</code> is missing, see <a href="https://github.com/intel/linux-npu-driver" target="_blank" rel="noreferrer">intel/linux-npu-driver</a>. Then pass the device through:</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">    devices</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">      - </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">/dev/accel:/dev/accel</span></span></code></pre></div><p>The OpenVINO plugin then lists <code>NPU</code> in its <code>Available devices:</code> log line and its device setting.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("install/hardware-acceleration.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const hardwareAcceleration = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  hardwareAcceleration as default
};
