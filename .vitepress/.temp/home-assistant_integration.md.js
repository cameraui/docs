import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Integration","description":"","frontmatter":{"title":"Integration"},"headers":[],"relativePath":"home-assistant/integration.md","filePath":"home-assistant/integration.md","lastUpdated":1785438411000}');
const _sfc_main = { name: "home-assistant/integration.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><div style="${ssrRenderStyle({ "display": "none" })}" hidden="true" aria-hidden="true" data-nosnippet>Are you an LLM? You can read better optimized documentation at /home-assistant/integration.md for this page in Markdown format</div><h1 id="home-assistant-integration" tabindex="-1">Home Assistant Integration <a class="header-anchor" href="#home-assistant-integration" aria-label="Permalink to &quot;Home Assistant Integration&quot;">​</a></h1><p>The <strong>camera.ui integration</strong> connects an existing camera.ui server to Home Assistant. Your cameras, their sensors, motion and object detection, PTZ controls and the full camera.ui interface all show up inside Home Assistant.</p><p>It talks to the server directly over your local network, so events arrive by push the moment they happen. You don&#39;t need an MQTT broker or any cloud service.</p><p>This page covers the custom integration (domain <code>cameraui</code>). If you run Home Assistant OS and want camera.ui to run as a managed app instead, see the <a href="/home-assistant/app">App</a> page.</p><h2 id="install" tabindex="-1">Install <a class="header-anchor" href="#install" aria-label="Permalink to &quot;Install&quot;">​</a></h2><p>The integration is distributed through <a href="https://hacs.xyz" target="_blank" rel="noreferrer">HACS</a> as a custom repository.</p><ol><li>In HACS, add <code>cameraui/homeassistant-integration</code> as a custom <strong>Integration</strong> repository.</li><li>Install <strong>camera.ui</strong> from HACS.</li><li>Restart Home Assistant.</li></ol><h2 id="connecting" tabindex="-1">Connecting <a class="header-anchor" href="#connecting" aria-label="Permalink to &quot;Connecting&quot;">​</a></h2><p>Once installed, Home Assistant needs to find your server and authenticate against it.</p><h3 id="discovery" tabindex="-1">Discovery <a class="header-anchor" href="#discovery" aria-label="Permalink to &quot;Discovery&quot;">​</a></h3><p>camera.ui advertises itself on the local network over mDNS (<code>_camera-ui._tcp.local.</code>). When Home Assistant sees it, a <strong>camera.ui found</strong> card appears under <strong>Settings → Devices &amp; services</strong>. Open it and enter your API token; the host and port are filled in from discovery.</p><h3 id="manual-setup" tabindex="-1">Manual setup <a class="header-anchor" href="#manual-setup" aria-label="Permalink to &quot;Manual setup&quot;">​</a></h3><p>If discovery doesn&#39;t reach the server (a different subnet, for example), add it by hand:</p><ol><li>Go to <strong>Settings → Devices &amp; services → Add integration</strong> and search for <strong>camera.ui</strong>.</li><li>Enter the <strong>Host</strong>, <strong>Port</strong> (default <code>3443</code>) and your <strong>API token</strong>.</li></ol><h3 id="api-token" tabindex="-1">API token <a class="header-anchor" href="#api-token" aria-label="Permalink to &quot;API token&quot;">​</a></h3><p>Both paths need an API token. Create one in camera.ui under <strong>Settings → Account → API tokens</strong>, then paste it into the integration.</p><p>Home Assistant checks the token against the server before finishing. If the token is rejected you&#39;ll see an authentication error; if the server can&#39;t be reached you&#39;ll see a connection error instead.<sup class="footnote-ref"><a href="#fn1" id="fnref1">[1]</a></sup></p><h2 id="what-you-get" tabindex="-1">What you get <a class="header-anchor" href="#what-you-get" aria-label="Permalink to &quot;What you get&quot;">​</a></h2><p>The integration creates <strong>one Home Assistant device per camera</strong> and keeps the list in sync. It re-reads the camera list every 60 seconds, and a camera you remove in camera.ui is removed from Home Assistant automatically.</p><h3 id="cameras" tabindex="-1">Cameras <a class="header-anchor" href="#cameras" aria-label="Permalink to &quot;Cameras&quot;">​</a></h3><p>Each camera device carries a <strong>camera entity</strong> that streams live and serves snapshots. The device is labelled with the manufacturer <code>camera.ui</code> and links back to the server&#39;s web interface.</p><h3 id="sensors" tabindex="-1">Sensors <a class="header-anchor" href="#sensors" aria-label="Permalink to &quot;Sensors&quot;">​</a></h3><p>Every <a href="/sensors/">exposed sensor</a> in camera.ui is mirrored into Home Assistant on the matching platform, whether it belongs to a camera or not:</p><table tabindex="0"><thead><tr><th>camera.ui sensor</th><th>Home Assistant platform</th></tr></thead><tbody><tr><td>Binary / contact</td><td><code>binary_sensor</code></td></tr><tr><td>Measurement</td><td><code>sensor</code></td></tr><tr><td>Switch</td><td><code>switch</code></td></tr><tr><td>Light</td><td><code>light</code></td></tr><tr><td>Siren</td><td><code>siren</code></td></tr><tr><td>Lock</td><td><code>lock</code></td></tr><tr><td>Cover</td><td><code>cover</code></td></tr><tr><td>Alarm system</td><td><code>alarm_control_panel</code></td></tr></tbody></table><p>Camera hardware (a camera&#39;s own spotlight, siren or battery) lands on the camera&#39;s device. Every other sensor becomes a device of its own, linked below its camera when it is assigned to exactly one. Whether a sensor comes across at all is the <strong>Expose sensor</strong> toggle on the <a href="/sensors/setup#the-sensors-page">Sensors page</a>.</p><p>The controllable ones work both ways: a <strong>switch</strong> and a <strong>siren</strong> turn on and off, a <strong>light</strong> turns on and off (and dims, if it reports brightness), a <strong>cover</strong> opens and closes, a <strong>lock</strong> locks and unlocks, and an <strong>alarm panel</strong> arms home, away or night and disarms. Commands are sent straight to camera.ui.</p><p>This surface is driven live. Add, rename, re-assign, expose or delete a sensor in camera.ui and the matching Home Assistant entity appears, updates or disappears without a restart. A sensor whose plugin is offline shows as unavailable.</p><h3 id="motion-and-object-detection" tabindex="-1">Motion and object detection <a class="header-anchor" href="#motion-and-object-detection" aria-label="Permalink to &quot;Motion and object detection&quot;">​</a></h3><p>Every camera gets a <strong>motion</strong> binary sensor that turns on while a detection is active.</p><p>Cameras with object detection also get one binary sensor each for <strong>person</strong>, <strong>vehicle</strong>, <strong>animal</strong> and <strong>package</strong>. Each turns on while that object is in view and clears when the detection ends.<sup class="footnote-ref"><a href="#fn2" id="fnref2">[2]</a></sup></p><h3 id="recognition-sensors" tabindex="-1">Recognition sensors <a class="header-anchor" href="#recognition-sensors" aria-label="Permalink to &quot;Recognition sensors&quot;">​</a></h3><p>Cameras with the matching detector get value sensors for <strong>face</strong>, <strong>license plate</strong> and <strong>classification</strong>. The sensor&#39;s value is the recognized labels, and the raw detections are available in its attributes.</p><h2 id="ptz" tabindex="-1">PTZ <a class="header-anchor" href="#ptz" aria-label="Permalink to &quot;PTZ&quot;">​</a></h2><p>Pan, tilt and zoom is exposed as a <strong>service</strong>, <code>cameraui.ptz</code>, rather than as an entity. Target one or more camera entities and pick an action:</p><ul><li><strong>continuous.</strong> Move at a set speed until stopped, using <code>pan</code>, <code>tilt</code> and <code>zoom</code> as speeds.</li><li><strong>stop.</strong> Stop a continuous move.</li><li><strong>move.</strong> Move by a relative step.</li><li><strong>absolute.</strong> Go to an absolute position.</li><li><strong>home.</strong> Return to the home position.</li><li><strong>preset.</strong> Recall a saved preset by name.</li></ul><p>The <code>pan</code>, <code>tilt</code> and <code>zoom</code> fields run from <code>-1</code> to <code>1</code>. The <code>preset</code> action needs a preset name. If none of the targeted cameras has PTZ, the service reports an error.</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">action</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  - </span><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">service</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">cameraui.ptz</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">    target</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">      entity_id</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">camera.driveway</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">    data</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">      action</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">preset</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">      preset</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">gate</span></span></code></pre></div><h2 id="automations" tabindex="-1">Automations <a class="header-anchor" href="#automations" aria-label="Permalink to &quot;Automations&quot;">​</a></h2><h3 id="device-triggers" tabindex="-1">Device triggers <a class="header-anchor" href="#device-triggers" aria-label="Permalink to &quot;Device triggers&quot;">​</a></h3><p>Each camera device offers three triggers in the automation editor:</p><ul><li><strong>Detection started.</strong> A detection began on the camera.</li><li><strong>Detection ended.</strong> The detection cleared.</li><li><strong>Object recognized.</strong> A face, plate or classification was recognized during a detection.</li></ul><h3 id="the-event-bus" tabindex="-1">The event bus <a class="header-anchor" href="#the-event-bus" aria-label="Permalink to &quot;The event bus&quot;">​</a></h3><p>The integration also fires a <code>cameraui_event</code> on the Home Assistant event bus. Unlike a single &quot;something was detected&quot; event, several of these fire across one detection&#39;s lifecycle:</p><table tabindex="0"><thead><tr><th><code>state</code></th><th>When</th></tr></thead><tbody><tr><td><code>start</code></td><td>A detection begins.</td></tr><tr><td><code>object</code></td><td>A new object label appears during the detection.</td></tr><tr><td><code>recognized</code></td><td>A face, plate or classification is recognized during the detection.</td></tr><tr><td><code>end</code></td><td>The detection clears.</td></tr></tbody></table><p>Each event carries these fields:</p><table tabindex="0"><thead><tr><th>Field</th><th>Contents</th></tr></thead><tbody><tr><td><code>camera_id</code></td><td>The camera&#39;s id.</td></tr><tr><td><code>camera_name</code></td><td>The camera&#39;s name.</td></tr><tr><td><code>state</code></td><td>One of the values above.</td></tr><tr><td><code>detection_types</code></td><td>The detection types involved.</td></tr><tr><td><code>event_id</code></td><td>The id shared by all events of one detection.</td></tr><tr><td><code>labels</code></td><td>Object labels seen.</td></tr><tr><td><code>faces</code></td><td>Recognized faces.</td></tr><tr><td><code>plates</code></td><td>Recognized license plates.</td></tr><tr><td><code>classifications</code></td><td>Other recognized attributes.</td></tr><tr><td><code>attributes</code></td><td>The full raw attribute list.</td></tr></tbody></table><p>Filter on <code>state</code> to act on a specific point in the lifecycle:</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">automation</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  - </span><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">alias</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">Notify on recognized face</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">    trigger</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">      - </span><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">platform</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">event</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">        event_type</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">cameraui_event</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">        event_data</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">          state</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">recognized</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">    action</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">      - </span><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">service</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">notify.mobile_app</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">        data</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">          message</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;-</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">            {{ trigger.event.data.faces | join(&#39;, &#39;) }}</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">            seen at {{ trigger.event.data.camera_name }}</span></span></code></pre></div><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">automation</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  - </span><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">alias</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">Turn on light when a person appears</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">    trigger</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">      - </span><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">platform</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">event</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">        event_type</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">cameraui_event</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">        event_data</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">          state</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">object</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">    condition</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">      - </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;{{ &#39;person&#39; in trigger.event.data.labels }}&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">    action</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">      - </span><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">service</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">light.turn_on</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">        target</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">          entity_id</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">light.driveway</span></span></code></pre></div><p>The device triggers cover <code>start</code>, <code>end</code> and <code>recognized</code>. There is no device trigger for the <code>object</code> state, so use the event bus for that one.<sup class="footnote-ref"><a href="#fn3" id="fnref3">[3]</a></sup></p><h2 id="sidebar-panel" tabindex="-1">Sidebar panel <a class="header-anchor" href="#sidebar-panel" aria-label="Permalink to &quot;Sidebar panel&quot;">​</a></h2><p>Setup adds a <strong>camera.ui</strong> entry to the Home Assistant sidebar that embeds the full camera.ui interface. The panel is <strong>admin only</strong>: it appears for administrator accounts and stays hidden for everyone else.</p><h2 id="lovelace-card" tabindex="-1">Lovelace card <a class="header-anchor" href="#lovelace-card" aria-label="Permalink to &quot;Lovelace card&quot;">​</a></h2><p>A camera.ui Lovelace card ships with the integration and registers itself as a resource, so it&#39;s ready to add to a dashboard without a manual install. The bundle provides a single-camera card and a grid card.</p><p>See the <a href="/home-assistant/card">Lovelace card</a> page for the card options and layouts.</p><h2 id="next-steps" tabindex="-1">Next steps <a class="header-anchor" href="#next-steps" aria-label="Permalink to &quot;Next steps&quot;">​</a></h2><ul><li><strong><a href="/home-assistant/app">App</a></strong> — run camera.ui itself as a Home Assistant OS app.</li><li><strong><a href="/home-assistant/card">Lovelace card</a></strong> — configure the bundled dashboard cards.</li><li><strong><a href="/sensors/setup">Sensors</a></strong> — assign sensors to a camera so they reach Home Assistant.</li></ul><hr class="footnotes-sep"><section class="footnotes"><ol class="footnotes-list"><li id="fn1" class="footnote-item"><p>The server is reached over HTTPS. The integration does not verify the certificate, so a self-signed camera.ui certificate works without extra setup. <a href="#fnref1" class="footnote-backref">↩︎</a></p></li><li id="fn2" class="footnote-item"><p>Motion and object binary sensors are registered with the <code>motion</code> device class, so they present in Home Assistant as motion sensors rather than as a per-object class. <a href="#fnref2" class="footnote-backref">↩︎</a></p></li><li id="fn3" class="footnote-item"><p>The three device triggers map to <code>start</code>, <code>end</code> and <code>recognized</code>. To react to a specific object label appearing mid-detection, listen for <code>cameraui_event</code> with <code>state: object</code> as shown above. <a href="#fnref3" class="footnote-backref">↩︎</a></p></li></ol></section></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("home-assistant/integration.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const integration = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  integration as default
};
