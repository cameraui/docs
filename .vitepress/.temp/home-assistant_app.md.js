import { resolveComponent, useSSRContext } from "vue";
import { ssrRenderAttrs, ssrRenderStyle, ssrRenderComponent } from "vue/server-renderer";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Home Assistant app","description":"","frontmatter":{"title":"Home Assistant app"},"headers":[],"relativePath":"home-assistant/app.md","filePath":"home-assistant/app.md","lastUpdated":1784660752000}');
const _sfc_main = { name: "home-assistant/app.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  const _component_Shot = resolveComponent("Shot");
  _push(`<div${ssrRenderAttrs(_attrs)}><div style="${ssrRenderStyle({ "display": "none" })}" hidden="true" aria-hidden="true" data-nosnippet>Are you an LLM? You can read better optimized documentation at /home-assistant/app.md for this page in Markdown format</div><h1 id="home-assistant-app" tabindex="-1">Home Assistant app <a class="header-anchor" href="#home-assistant-app" aria-label="Permalink to &quot;Home Assistant app&quot;">​</a></h1><p>You can run the full camera.ui server inside Home Assistant OS as an app, with no separate machine to maintain. There are two apps in the repository: <strong>camera.ui</strong>, the full server with its own panel in the Home Assistant sidebar, and <strong>camera.ui Worker</strong>, a helper that lends this machine&#39;s compute to a camera.ui server running elsewhere. Both are supported on <code>amd64</code> and <code>aarch64</code> hosts.</p><h2 id="install" tabindex="-1">Install <a class="header-anchor" href="#install" aria-label="Permalink to &quot;Install&quot;">​</a></h2><ol><li>In Home Assistant, open <strong>Settings → Apps → App Store</strong>.</li><li>From the store&#39;s menu (top right), choose <strong>Repositories</strong> and add the camera.ui app repository URL.</li><li>Find <strong>camera.ui</strong> in the store and click <strong>Install</strong>.</li></ol>`);
  _push(ssrRenderComponent(_component_Shot, {
    src: "/img/home-assistant/app-store.png",
    alt: "The camera.ui app in the Home Assistant App Store"
  }, null, _parent));
  _push(`<h2 id="the-camera-ui-app" tabindex="-1">The camera.ui app <a class="header-anchor" href="#the-camera-ui-app" aria-label="Permalink to &quot;The camera.ui app&quot;">​</a></h2><p>The <strong>camera.ui</strong> app runs the complete server: the UI, the streaming engine, the NVR, and any plugins you install. It runs on the host network, which it needs for WebRTC live view, HomeKit pairing, and camera discovery.</p><p>Once started, the app adds a <strong>camera.ui</strong> entry to the Home Assistant sidebar. That panel, and the app&#39;s <strong>Open Web UI</strong> button, both open the interface through Home Assistant&#39;s ingress proxy, so there is no separate login and no certificate warning.<sup class="footnote-ref"><a href="#fn1" id="fnref1">[1]</a></sup></p><p>On <strong>first start</strong> the app downloads the camera.ui server and the default plugin, which needs an internet connection and takes a few minutes. Later starts are fast.<sup class="footnote-ref"><a href="#fn2" id="fnref2">[2]</a></sup> Follow the first boot in the app&#39;s <strong>Log</strong> tab.</p><h2 id="options" tabindex="-1">Options <a class="header-anchor" href="#options" aria-label="Permalink to &quot;Options&quot;">​</a></h2><p>The camera.ui app has three options.</p><ul><li><strong>default_plugins.</strong> Comma-separated plugins installed on first boot. Defaults to <code>@camera.ui/camera-ui-nvr</code>, the NVR plugin.</li><li><strong>recordings_path.</strong> Where the NVR plugin writes recordings. Defaults to <code>/media/camera.ui</code>. Paths under <code>/media</code> show up in the Home Assistant media browser.</li><li><strong>internal_avahi.</strong> Runs the app&#39;s own mDNS stack for HomeKit pairing and camera discovery. On by default. Turn it off to use the host&#39;s mDNS instead.</li></ul><h2 id="hardware-acceleration" tabindex="-1">Hardware acceleration <a class="header-anchor" href="#hardware-acceleration" aria-label="Permalink to &quot;Hardware acceleration&quot;">​</a></h2><p>The app passes GPU, AI accelerator, and codec devices through to the container. What is usable depends on your architecture and hardware.</p><ul><li><strong>Intel / AMD (VA-API).</strong> VA-API drivers ship on <code>amd64</code> only, covering Intel Quick Sync and AMD Mesa VA-API decoding. <code>aarch64</code> hosts do not get VA-API.</li><li><strong>Coral Edge TPU.</strong> The Coral runtime is included for both architectures. USB Coral works through the app&#39;s USB access; PCIe Coral uses the passed-through <code>/dev/apex_*</code> devices.</li><li><strong>Hailo.</strong> The Hailo device (<code>/dev/hailo0</code>) is passed through, but the Hailo runtime is not part of the image. It comes with the Hailo plugin.<sup class="footnote-ref"><a href="#fn3" id="fnref3">[3]</a></sup></li><li><strong>NVIDIA.</strong> Not available in the Home Assistant app. Use <a href="/install/docker">Docker</a> on a plain Linux host for an NVIDIA setup.</li></ul><p>The <a href="/install/hardware-acceleration">Hardware acceleration</a> page explains how the streaming engine picks a device and how to check that it works.</p><h2 id="recordings-and-the-media-browser" tabindex="-1">Recordings and the media browser <a class="header-anchor" href="#recordings-and-the-media-browser" aria-label="Permalink to &quot;Recordings and the media browser&quot;">​</a></h2><p>The <code>/media</code> directory is mapped into the app with read-write access. With the default <code>recordings_path</code> of <code>/media/camera.ui</code>, recordings written by the NVR appear in Home Assistant&#39;s <strong>Media</strong> browser alongside your other media.</p><h2 id="backups-and-updates" tabindex="-1">Backups and updates <a class="header-anchor" href="#backups-and-updates" aria-label="Permalink to &quot;Backups and updates&quot;">​</a></h2><p>Home Assistant backs up the app&#39;s <code>/data</code> directory, which holds your configuration, database, and TLS certificates. Three subtrees are <strong>excluded</strong> from backups: the downloaded server, the Python environment, and installed plugins. These are re-downloaded on restore, so restoring a backup needs an internet connection, the same as a first start.</p><p>The server updates itself from within the app, so day-to-day updates do not need a new app image. Update the app itself when a new version is published to refresh the base image (OS, drivers, and launcher).</p><p>Leave the supervisor&#39;s <strong>Watchdog</strong> toggle on for the app so Home Assistant restarts it if it stops unexpectedly.<sup class="footnote-ref"><a href="#fn4" id="fnref4">[4]</a></sup></p><h2 id="worker-app" tabindex="-1">Worker app <a class="header-anchor" href="#worker-app" aria-label="Permalink to &quot;Worker app&quot;">​</a></h2><p>The <strong>camera.ui Worker</strong> app turns this Home Assistant machine into a <a href="/admin/workers">worker</a> for a camera.ui server (the master) running somewhere else. It lends this machine&#39;s CPU, GPU, and Coral to the master for decoding, detection, and plugins. It has no UI and no sidebar panel of its own; it dials out to the master and waits for work. It passes through the same hardware as the full app.</p><p>Set it up on the master first, then here:</p><ol><li>On the <strong>master</strong>, enable workers and set the master address, then generate a one-time pairing code. Generating a code and pairing both fail unless workers are enabled there. See <a href="/admin/workers">Workers</a>.</li><li>Install the <strong>camera.ui Worker</strong> app on this machine.</li><li>Set <strong>master_address</strong> to the master&#39;s IP or hostname and <strong>pairing_code</strong> to the code you just generated.</li><li>Start the app. It exchanges the code for its own credentials on first connect and reconnects on its own afterwards, so the code is entered only once.</li></ol>`);
  _push(ssrRenderComponent(_component_Shot, {
    src: "/img/home-assistant/worker-options.png",
    alt: "The camera.ui Worker app options"
  }, null, _parent));
  _push(`<p>The worker app has four options.</p><ul><li><strong>master_address.</strong> IP or hostname of the camera.ui master. Required.</li><li><strong>pairing_code.</strong> The one-time code generated on the master. Required for the first connection. A code is valid for 15 minutes and can be used once.</li><li><strong>worker_name.</strong> Name shown for this worker on the master. Optional. Defaults to <code>worker</code>.</li><li><strong>master_api_port.</strong> The master&#39;s HTTPS API port. Optional. Only needed if the master&#39;s API runs on a non-default port; the default is <code>3443</code>.</li></ul><h2 id="next-steps" tabindex="-1">Next steps <a class="header-anchor" href="#next-steps" aria-label="Permalink to &quot;Next steps&quot;">​</a></h2><ul><li><strong><a href="/install/hardware-acceleration">Hardware acceleration</a></strong> — how the streaming engine uses your GPU and accelerators, and how to verify it.</li><li><strong><a href="/admin/workers">Workers</a></strong> — the full picture of masters, workers, and camera assignments.</li><li><strong><a href="/install/docker">Docker</a></strong> — run the server outside Home Assistant, including NVIDIA setups.</li></ul><hr class="footnotes-sep"><section class="footnotes"><ol class="footnotes-list"><li id="fn1" class="footnote-item"><p>The app also listens on <code>https://&lt;host&gt;:3443</code> directly over the host network. Reaching it that way shows a self-signed certificate warning once, while the sidebar panel and Open Web UI button do not. <a href="#fnref1" class="footnote-backref">↩︎</a></p></li><li id="fn2" class="footnote-item"><p>If first boot fails to fetch the server, check that the host can reach the internet and resolve DNS. <a href="#fnref2" class="footnote-backref">↩︎</a></p></li><li id="fn3" class="footnote-item"><p>Install the Hailo plugin to add the runtime. The app image itself only passes the device through. <a href="#fnref3" class="footnote-backref">↩︎</a></p></li><li id="fn4" class="footnote-item"><p>Watchdog is a Home Assistant setting on the app&#39;s info page, not a camera.ui option. <a href="#fnref4" class="footnote-backref">↩︎</a></p></li></ol></section></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("home-assistant/app.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const app = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  app as default
};
