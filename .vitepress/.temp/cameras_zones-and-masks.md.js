import { resolveComponent, useSSRContext } from "vue";
import { ssrRenderAttrs, ssrRenderStyle, ssrRenderComponent } from "vue/server-renderer";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Zones & lines","description":"","frontmatter":{"title":"Zones & lines"},"headers":[],"relativePath":"cameras/zones-and-masks.md","filePath":"cameras/zones-and-masks.md","lastUpdated":1785095507000}');
const _sfc_main = { name: "cameras/zones-and-masks.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  const _component_Shot = resolveComponent("Shot");
  _push(`<div${ssrRenderAttrs(_attrs)}><div style="${ssrRenderStyle({ "display": "none" })}" hidden="true" aria-hidden="true" data-nosnippet>Are you an LLM? You can read better optimized documentation at /cameras/zones-and-masks.md for this page in Markdown format</div><h1 id="zones-lines" tabindex="-1">Zones &amp; lines <a class="header-anchor" href="#zones-lines" aria-label="Permalink to &quot;Zones &amp; lines&quot;">​</a></h1><p>Zones let you control where detection happens, ignore parts of the frame, and detect when something crosses a line. You edit them per camera, and they act on the camera&#39;s <a href="/detection/">detection</a>, so it needs detection set up.</p><h2 id="opening-the-editor" tabindex="-1">Opening the editor <a class="header-anchor" href="#opening-the-editor" aria-label="Permalink to &quot;Opening the editor&quot;">​</a></h2><p>Open a camera&#39;s <a href="/cameras/settings">settings</a> and go to the <strong>Settings</strong> tab. Under <strong>Zones</strong> you see the zones and lines already on the camera, each with its colour, name and type (Zone, Ignore zone or Line crossing). The pencil on an entry opens the editor on it, the bin deletes it. With nothing configured yet, you only see &quot;No zones or lines on this camera yet.&quot;</p><p>Below the list, <strong>Edit zones</strong> opens the editor over the camera image with two tabs: <strong>Zones</strong> and <strong>Lines</strong>.</p>`);
  _push(ssrRenderComponent(_component_Shot, {
    src: "/img/cameras/zone-editor.png",
    alt: "Zone editor in the Zones tab"
  }, null, _parent));
  _push(`<h2 id="detection-zones" tabindex="-1">Detection zones <a class="header-anchor" href="#detection-zones" aria-label="Permalink to &quot;Detection zones&quot;">​</a></h2><p>On the <strong>Zones</strong> tab, draw a polygon by adding points on the image. Each zone is either an include or an exclude zone:</p><ul><li><strong>Include.</strong> Only detections inside the zone count. Everything else in the frame is ignored. Use it to watch one spot, like a driveway or a doorway.</li><li><strong>Exclude.</strong> Detections inside the zone are ignored, everything outside the zone still counts. Use it to cut out one troublesome corner, like a swaying tree, while the rest of the frame keeps working.</li></ul><p>One exclude zone on its own is all you need to say &quot;watch everything except this&quot;. You don&#39;t have to add an include zone around the rest.</p><h3 id="combining-zones" tabindex="-1">Combining zones <a class="header-anchor" href="#combining-zones" aria-label="Permalink to &quot;Combining zones&quot;">​</a></h3><p>Draw several include zones and a detection counts when it is in any one of them. Draw several exclude zones and each one cuts its area out.</p><p>If you mix both, <strong>exclude wins</strong>: a detection inside an exclude zone is dropped even when it also sits in an include zone. That is what lets you carve a hole out of a larger include zone, for example watching the whole front yard but not the pavement running through it.</p><p>Note the change once an include zone exists: being outside every exclude zone is no longer enough, a detection now also has to be inside an include zone. Only exclude zones on the camera means everything outside them counts.</p><h3 id="match-mode" tabindex="-1">Match mode <a class="header-anchor" href="#match-mode" aria-label="Permalink to &quot;Match mode&quot;">​</a></h3><p><strong>Contain</strong> is the default: a detection counts as inside only when its whole box is in the zone. <strong>Intersect</strong> counts it as soon as the box overlaps the zone at all.</p><p>The mode decides what &quot;inside&quot; means for both kinds of zone. With an exclude zone in Contain mode, someone standing half in your excluded area is still detected. Switch that zone to Intersect if you want it gone as soon as it touches the area.</p><h3 id="object-types" tabindex="-1">Object types <a class="header-anchor" href="#object-types" aria-label="Permalink to &quot;Object types&quot;">​</a></h3><p>You can limit a zone to specific <strong>object types</strong>, so it reacts only to people or only to vehicles.</p><p>Object types are not local to the zone though. They are collected across all zones of a camera and act as one allow-list for the whole camera. Limit any zone to vehicles and person detections are dropped everywhere on that camera, including inside zones that have no object types of their own. That holds for exclude zones too, so an exclude zone set to vehicles switches off person detection on the entire camera, which is rarely what you want.</p><p>Every new zone starts with <strong>motion, person, vehicle and animal</strong> already selected. If you only want to limit the area and not the object types, clear the selection on each zone. Leaving the default in place also means package detections are dropped, since package is not part of it.</p><h2 id="ignore-zones" tabindex="-1">Ignore zones <a class="header-anchor" href="#ignore-zones" aria-label="Permalink to &quot;Ignore zones&quot;">​</a></h2><p>Turn a zone into an <strong>ignore zone</strong> to leave an area out of detection completely, for example a neighbour&#39;s window or a public pavement. Detections that sit fully inside it are dropped and never trigger an event. Something that only partly overlaps it still counts.</p><p>An ignore zone has no settings of its own, and that is the point of it. It always uses the &quot;fully inside&quot; rule, it drops whatever lands there no matter the object type, and it stays out of the camera-wide allow-list described above. When you want an area gone and nothing else, that makes it the safer choice over an exclude zone. Reach for an exclude zone instead when you need it to interact with your include zones, or when you want Intersect rather than Contain.</p><p>An ignore zone does not change the video.<sup class="footnote-ref"><a href="#fn1" id="fnref1">[1]</a></sup> The area stays visible in live view and in recordings, and it is still recorded.</p><h2 id="lines-line-crossing" tabindex="-1">Lines (line-crossing) <a class="header-anchor" href="#lines-line-crossing" aria-label="Permalink to &quot;Lines (line-crossing)&quot;">​</a></h2><p>On the <strong>Lines</strong> tab, draw a line across the image to detect when something crosses it. Each line has a <strong>direction</strong>:</p><ul><li><strong>A → B</strong> or <strong>B → A</strong> triggers on a crossing in one direction.</li><li><strong>A ↔ B</strong> triggers in either direction.</li></ul><p>Like zones, a line can be limited to specific object types. A crossing fires an event only when something actually moves across the line in the chosen direction, so you can alert on someone coming through a gate rather than just appearing in view.</p><hr class="footnotes-sep"><section class="footnotes"><ol class="footnotes-list"><li id="fn1" class="footnote-item"><p>camera.ui has no way to black out part of the picture. If a neighbour&#39;s window must not be filmed at all, point the camera elsewhere, or use your camera&#39;s own privacy mask if it has one, which blanks the area before the picture reaches camera.ui. <a href="#fnref1" class="footnote-backref">↩︎</a></p></li></ol></section></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("cameras/zones-and-masks.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const zonesAndMasks = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  zonesAndMasks as default
};
