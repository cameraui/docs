import { resolveComponent, useSSRContext } from "vue";
import { ssrRenderAttrs, ssrRenderStyle, ssrRenderSuspense, ssrRenderComponent } from "vue/server-renderer";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"How it works","description":"","frontmatter":{},"headers":[],"relativePath":"intro/architecture.md","filePath":"intro/architecture.md","lastUpdated":1784139260000}');
const _sfc_main = { name: "intro/architecture.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  const _component_Mermaid = resolveComponent("Mermaid");
  _push(`<div${ssrRenderAttrs(_attrs)}><div style="${ssrRenderStyle({ "display": "none" })}" hidden="true" aria-hidden="true" data-nosnippet>Are you an LLM? You can read better optimized documentation at /intro/architecture.md for this page in Markdown format</div><h1 id="how-it-works" tabindex="-1">How it works <a class="header-anchor" href="#how-it-works" aria-label="Permalink to &quot;How it works&quot;">​</a></h1><p>camera.ui is <strong>server-centric</strong>: one server, running on your hardware, does all the work. It connects to your cameras, streams and records video, runs detection, and serves the interface. The browser and the mobile apps are <strong>viewers</strong> of that server. The desktop app can be a viewer too, or run the server itself.</p><p>You don&#39;t need to understand the internals to use camera.ui, but a quick mental model makes the rest of these docs easier to follow.</p><h2 id="one-self-contained-server" tabindex="-1">One self-contained server <a class="header-anchor" href="#one-self-contained-server" aria-label="Permalink to &quot;One self-contained server&quot;">​</a></h2><p>The camera.ui server bundles everything it needs to run: the web interface, a built-in <strong>streaming engine</strong> (our own go2rtc build) for live video, and its own <strong>video processing</strong> for decoding and recording. Your settings, cameras, users, and events live in a small <strong>local database</strong> on the same machine.</p><p>Nothing leaves your network unless you turn on <a href="/remote/">remote access</a>. There is no external database, and no cloud account is required to run a server.</p><h2 id="the-big-picture" tabindex="-1">The big picture <a class="header-anchor" href="#the-big-picture" aria-label="Permalink to &quot;The big picture&quot;">​</a></h2>`);
  ssrRenderSuspense(_push, {
    default: () => {
      _push(ssrRenderComponent(_component_Mermaid, {
        id: "mermaid-22",
        class: "mermaid",
        graph: "flowchart%20LR%0A%20%20cam%5BCameras%3Cbr%2F%3ERTSP%20%C2%B7%20ONVIF%5D%0A%0A%20%20subgraph%20server%5Bcamera.ui%20server%20%C2%B7%20your%20hardware%5D%0A%20%20%20%20direction%20TB%0A%20%20%20%20g%5BStreaming%20engine%5D%20--%3E%20fw%5BFrame%20worker%3Cbr%2F%3Eone%20per%20camera%5D%0A%20%20%20%20fw%20--%3E%20det%5BDetection%3Cbr%2F%3Emotion%20%E2%86%92%20object%20%E2%86%92%20face%20%C2%B7%20plate%20%C2%B7%20audio%5D%0A%20%20%20%20det%20--%3E%20nvr%5B(NVR%20plugin%3Cbr%2F%3Erecordings%20%2B%20push)%5D%0A%20%20%20%20g%20--%3E%20api%5BWeb%20UI%20%2B%20API%5D%0A%20%20%20%20nvr%20--%3E%20api%0A%20%20end%0A%0A%20%20cam%20--%3E%20g%0A%20%20api%20--%3E%20viewers%5BViewers%3Cbr%2F%3Eweb%20%C2%B7%20desktop%20%C2%B7%20mobile%5D%0A%20%20api%20-.%20optional%20.-%3E%20remote%5BRemote%20access%3Cbr%2F%3ECloud%20%C2%B7%20tunnel%5D%0A%20%20remote%20-.%20from%20anywhere%20.-%3E%20viewers%0A"
      }, null, _parent));
    },
    fallback: () => {
      _push(` Loading... `);
    },
    _: 1
  });
  _push(`<h2 id="from-camera-to-notification" tabindex="-1">From camera to notification <a class="header-anchor" href="#from-camera-to-notification" aria-label="Permalink to &quot;From camera to notification&quot;">​</a></h2><p>Here is what happens behind a single detection:</p><ol><li><strong>Streaming.</strong> The streaming engine connects to each camera and turns its feed into browser-friendly live video (WebRTC and MSE). Streams are kept &quot;warm&quot; so live view and snapshots load instantly.</li><li><strong>Per-camera analysis.</strong> Every camera gets its own <strong>frame worker</strong>, a dedicated background process that decodes the video and runs detection. Because each camera runs on its own, a problem with one never affects the others.</li><li><strong>Layered detection.</strong> Cheap motion detection runs first. Only when it sees movement does it wake the heavier AI: object detection, then faces, license plates, classification, and semantic (CLIP) analysis. Audio is analyzed in parallel. This &quot;cascade&quot; keeps CPU and GPU use low.<sup class="footnote-ref"><a href="#fn1" id="fnref1">[1]</a></sup></li><li><strong>Events &amp; recording.</strong> When detection fires, the server builds an <strong>event</strong> with segments, thumbnails, and the objects, faces, or plates it found. The <strong>NVR plugin</strong> records the footage, stores it, and serves it back for playback.<sup class="footnote-ref"><a href="#fn2" id="fnref2">[2]</a></sup></li><li><strong>Notifications.</strong> Events can trigger push notifications and run <a href="/automations/">automations</a>.<sup class="footnote-ref"><a href="#fn2" id="fnref2:1">[2:1]</a></sup></li></ol><h2 id="plugins-make-it-extensible" tabindex="-1">Plugins make it extensible <a class="header-anchor" href="#plugins-make-it-extensible" aria-label="Permalink to &quot;Plugins make it extensible&quot;">​</a></h2><p>A lot of what camera.ui does is delivered by <strong>plugins</strong>, add-ons you install from an in-app store. Each plugin runs in its own isolated process, so a misbehaving plugin can&#39;t take down the server, and it restarts automatically if it crashes.</p><p>Plugins provide:</p><ul><li><strong>Camera sources.</strong> ONVIF and other camera protocols.</li><li><strong>Detectors.</strong> Motion engines and the AI backends (CoreML, ONNX, OpenVINO, NCNN, Coral, Hailo).</li><li><strong>Recording &amp; notifications.</strong> The NVR plugin records your footage and sends push notifications.</li><li><strong>Smart-home bridges.</strong> Apple HomeKit.</li></ul><p>Learn more under <a href="/plugins/">Plugins</a>.</p><h2 id="apps-desktop-mobile-web" tabindex="-1">Apps: desktop, mobile, web <a class="header-anchor" href="#apps-desktop-mobile-web" aria-label="Permalink to &quot;Apps: desktop, mobile, web&quot;">​</a></h2><p>You use camera.ui through the same interface everywhere, but the apps don&#39;t all play the same role:</p><ul><li>The <strong><a href="/install/desktop">desktop app</a></strong> can be the <strong>server itself</strong> (running camera.ui on your machine, the simplest all-in-one setup), a <strong>viewer</strong> that connects to another server, or a <strong>worker</strong> that helps another server with decoding, detection, or a plugin. You choose on first launch, and can switch anytime. For worker setup, see <a href="#scaling-across-machines">Scaling across machines</a>.</li><li>The <strong><a href="/install/mobile">mobile apps</a></strong> and the <strong>browser</strong> are always <strong>viewers</strong>.</li></ul><p>How viewers reach the server:</p><ul><li>On your network, the browser and a desktop viewer connect <strong>directly</strong>.</li><li>The <strong>mobile apps</strong> always connect through <strong>camera.ui Cloud</strong>.<sup class="footnote-ref"><a href="#fn3" id="fnref3">[3]</a></sup></li><li>A browser away from home uses whichever <a href="/remote/">remote access</a> option you set up: camera.ui Cloud, a Cloudflare tunnel, or your own domain.</li></ul><p>You can also save more than one server as an <strong>Instance</strong> and switch between them from the same app.</p><h2 id="scaling-across-machines" tabindex="-1">Scaling across machines <a class="header-anchor" href="#scaling-across-machines" aria-label="Permalink to &quot;Scaling across machines&quot;">​</a></h2><p>For larger setups, you can add extra machines as <strong>workers</strong>. A worker takes over the decoding and detection for some cameras, offloading the main server, or it can run an entire plugin instead (useful for a detector that needs specific hardware the main server lacks). Cameras and plugins assigned to a worker automatically <strong>fall back</strong> to the main server if that worker goes offline, and move back to the worker once it reconnects. See <a href="/admin/workers">Workers</a>.</p><h2 id="reaching-it-from-outside" tabindex="-1">Reaching it from outside <a class="header-anchor" href="#reaching-it-from-outside" aria-label="Permalink to &quot;Reaching it from outside&quot;">​</a></h2><p>On your local network you connect directly. To reach your server from anywhere, camera.ui offers several options: camera.ui Cloud, Cloudflare tunnels, a custom domain, or direct port-forwarding. All of them are optional and entirely your choice. See <a href="/remote/">Remote access</a>.</p><hr class="footnotes-sep"><section class="footnotes"><ol class="footnotes-list"><li id="fn1" class="footnote-item"><p>Detection needs a detection plugin that matches your hardware (CoreML, ONNX, OpenVINO, NCNN, or an edge accelerator like Coral or Hailo). See <a href="/detection/">Detection &amp; AI</a>. <a href="#fnref1" class="footnote-backref">↩︎</a></p></li><li id="fn2" class="footnote-item"><p>An active camera.ui subscription covers recording (NVR) and the features built on it, such as playback, export, face recognition, semantic search, and AI descriptions, plus push notifications. Live view and real-time detection are free. <a href="#fnref2" class="footnote-backref">↩︎</a> <a href="#fnref2:1" class="footnote-backref">↩︎</a></p></li><li id="fn3" class="footnote-item"><p>camera.ui Cloud is optional. On your own network everything stays local, and your server never has to connect to the cloud. See <a href="#reaching-it-from-outside">Reaching it from outside</a>. <a href="#fnref3" class="footnote-backref">↩︎</a></p></li></ol></section></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("intro/architecture.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const architecture = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  architecture as default
};
