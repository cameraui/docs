import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Proxmox","description":"","frontmatter":{"title":"Proxmox"},"headers":[],"relativePath":"de/install/proxmox.md","filePath":"de/install/proxmox.md","lastUpdated":1785095507000}');
const _sfc_main = { name: "de/install/proxmox.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><div style="${ssrRenderStyle({ "display": "none" })}" hidden="true" aria-hidden="true" data-nosnippet>Are you an LLM? You can read better optimized documentation at /de/install/proxmox.md for this page in Markdown format</div><h1 id="proxmox" tabindex="-1">Proxmox <a class="header-anchor" href="#proxmox" aria-label="Permalink to &quot;Proxmox&quot;">​</a></h1><p>camera.ui läuft auf Proxmox VE in einem LXC-Container, der Docker beherbergt. Ein Befehl auf dem Proxmox-Host richtet alles ein: Container, Docker und camera.ui.</p><div class="tip custom-block"><p class="custom-block-title">Warum ein LXC und keine VM?</p><p>Ein LXC kann die iGPU <strong>teilen</strong>: <code>/dev/dri</code> bleibt gleichzeitig für den Host und andere Container nutzbar, während eine VM mit PCIe-Passthrough die GPU monopolisiert. Zusammen mit dem geringeren Overhead ist das das Standard-Muster für NVRs auf Proxmox. Für NVIDIA-Karten ist eine VM die bessere Wahl (siehe unten).</p></div><h2 id="schnellstart" tabindex="-1">Schnellstart <a class="header-anchor" href="#schnellstart" aria-label="Permalink to &quot;Schnellstart&quot;">​</a></h2><p>Auf dem Proxmox-Host (als root) ausführen:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">curl</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -fsSLO</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> https://raw.githubusercontent.com/cameraui/docker/main/proxmox/install-cameraui-lxc.sh</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">bash</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> install-cameraui-lxc.sh</span></span></code></pre></div><p>Das Script lädt das Ubuntu-24.04-LXC-Template, erstellt einen <strong>unprivilegierten</strong> Container mit Nesting (der empfohlene Weg für Docker auf Proxmox, da privilegierte Container auf aktuellen Proxmox-Versionen Dockers AppArmor-Profil nicht mehr laden können), installiert Docker darin, deployt camera.ui via Docker Compose und gibt die URL der Web-UI aus. Getestet mit Proxmox VE 9.</p><p>Alles ist über Umgebungsvariablen einstellbar:</p><table tabindex="0"><thead><tr><th>Variable</th><th>Default</th><th>Bedeutung</th></tr></thead><tbody><tr><td><code>CTID</code></td><td>nächste freie ID</td><td>Container-ID</td></tr><tr><td><code>CT_HOSTNAME</code></td><td><code>cameraui</code></td><td>Container-Hostname</td></tr><tr><td><code>CORES</code> / <code>RAM_MB</code> / <code>DISK_GB</code></td><td><code>4</code> / <code>4096</code> / <code>16</code></td><td>Container-Ressourcen</td></tr><tr><td><code>BRIDGE</code></td><td><code>vmbr0</code></td><td>Netzwerk-Bridge (Container bekommt seine IP per DHCP)</td></tr><tr><td><code>STORAGE</code></td><td>auto-detect</td><td>rootfs-Storage (bevorzugt <code>local-lvm</code>, <code>local-zfs</code>, <code>local</code>, sonst den ersten aktiven Storage, der ein Container-rootfs aufnehmen kann)</td></tr><tr><td><code>TEMPLATE_STORAGE</code></td><td><code>local</code></td><td>Storage, auf den das LXC-Template geladen wird (braucht Content-Typ „Container-Template“)</td></tr><tr><td><code>FLAVOR</code></td><td><code>cpu</code></td><td><code>cpu</code>, <code>intel</code> oder <code>amd</code> (wählt den <a href="/de/install/docker#hardware-beschleunigung">Image-Flavor</a>); <code>nvidia</code> ist <a href="#nvidia-im-lxc-experimentell">experimentell</a></td></tr><tr><td><code>GPU_PASSTHROUGH</code></td><td><code>1</code> wenn Flavor ≠ cpu</td><td>reicht <code>/dev/dri</code> in den Container</td></tr><tr><td><code>TZ</code></td><td>Zeitzone des Hosts</td><td>Zeitzone im Container</td></tr></tbody></table><p>Beispiel für eine Intel-Maschine: <code>FLAVOR=intel bash install-cameraui-lxc.sh</code></p><h2 id="shell-im-container-offnen" tabindex="-1">Shell im Container öffnen <a class="header-anchor" href="#shell-im-container-offnen" aria-label="Permalink to &quot;Shell im Container öffnen&quot;">​</a></h2><p>Das Script legt den Container ohne Root-Passwort an, über die Proxmox-Konsole oder SSH kommst du also nicht hinein. Nimm stattdessen die Shell des Hosts:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pct</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> enter</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> &lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">CTI</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">D</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span></span></code></pre></div><p>Damit landest du in einer Root-Shell im Container. Für einzelne Befehle musst du gar nicht erst hineinwechseln:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pct</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> exec</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> &lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">CTI</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">D</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> docker</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> logs</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> cameraui</span></span></code></pre></div><p>Wenn du dich lieber wie gewohnt anmelden möchtest, vergib einmal ein Passwort. Danach funktionieren Konsole und SSH:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pct</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> exec</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> &lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">CTI</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">D</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> passwd</span></span></code></pre></div><h2 id="hardware-beschleunigung" tabindex="-1">Hardware-Beschleunigung <a class="header-anchor" href="#hardware-beschleunigung" aria-label="Permalink to &quot;Hardware-Beschleunigung&quot;">​</a></h2><ul><li><strong>Intel / AMD iGPU.</strong> <code>FLAVOR=intel</code> (oder <code>amd</code>) setzen. Das Script reicht den GPU-Render-Node über Proxmox&#39; Device-Passthrough in den Container und verdrahtet ihn bis zu Docker durch. Sonst nichts zu konfigurieren; die Details stehen auf der Seite <a href="/de/install/hardware-acceleration">Hardware-Beschleunigung</a>.</li><li><strong>NVIDIA.</strong> Nutze eine <strong>VM mit PCIe-Passthrough</strong> statt eines LXC. In der VM hast du ein normales Linux und folgst dem Standard-<a href="/de/install/docker">Docker-Setup</a> mit dem <code>nvidia</code>-Flavor. Ein LXC würde erfordern, den NVIDIA-Treiber auf dem Proxmox-Host und die Userspace-Bibliotheken im Container dauerhaft versionsgleich zu halten. Eine VM vermeidet das komplett, und Proxmox&#39; VM-Passthrough ist ausgezeichnet. Der Trade-off: Die VM monopolisiert die Karte. Brauchen andere Dienste auf dem Host dieselbe GPU (Ollama, Jellyfin), siehe <a href="#nvidia-im-lxc-experimentell">NVIDIA im LXC</a> unten.</li><li><strong>KI-Beschleuniger (Coral, Hailo).</strong> Gleiches Muster wie überall: den <a href="/de/install/hardware-acceleration#host-prüfen">Host-Treiber</a> auf dem Proxmox-Host installieren, dann den Device-Node (<code>/dev/apex_0</code>, <code>/dev/hailo0</code>) in den Container reichen.</li></ul><h3 id="nvidia-im-lxc-experimentell" tabindex="-1">NVIDIA im LXC (experimentell) <a class="header-anchor" href="#nvidia-im-lxc-experimentell" aria-label="Permalink to &quot;NVIDIA im LXC (experimentell)&quot;">​</a></h3><p><code>FLAVOR=nvidia</code> teilt die Karte zwischen dem Container und anderen Diensten auf dem Host, was eine VM nicht kann. Der Haken ist eine harte Versionskopplung: Die NVIDIA-Userspace-Bibliotheken im Container müssen exakt zum Kernel-Treiber des Hosts passen, nach jedem Treiber-Update.</p><p>Voraussetzung ist ein funktionierender NVIDIA-Treiber auf dem Proxmox-Host (<code>.run</code>-Installer mit <code>--dkms</code> plus <code>pve-headers</code>; <code>nvidia-smi</code> auf dem Host muss funktionieren), und die installierte Treiberversion muss auf <code>download.nvidia.com</code> existieren. Dann:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">FLAVOR</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">nvidia</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> bash</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> install-cameraui-lxc.sh</span></span></code></pre></div><p>Das Script reicht alle <code>/dev/nvidia*</code>-Device-Nodes in den Container, installiert den passenden Userspace-Treiber (<code>--no-kernel-module</code>) plus das NVIDIA Container Toolkit (mit <code>no-cgroups=true</code>, weil ein unprivilegierter Container keine Device-Cgroups verwalten kann) und registriert einen Sync-Service, der nach Host-Treiber-Updates beim Boot den passenden Userspace nachinstalliert. Verifizieren mit:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pct</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> exec</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> &lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">CTI</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">D</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> docker</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> exec</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> cameraui</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> nvidia-smi</span></span></code></pre></div><div class="warning custom-block"><p class="custom-block-title">Experimentell</p><p>Dieser Weg hängt an NVIDIAs Installer-Archiv und daran, dass der Host-Treiber gesund bleibt; nach einem Host-Treiber-Update braucht der Container einen Reboot zum Nachsynchronisieren. Wenn es bricht, funktioniert der VM-Weg immer. Reports und Fixes sind im <a href="https://github.com/cameraui/docker" target="_blank" rel="noreferrer">docker-Repo</a> willkommen.</p></div><h2 id="aufnahmen-auf-einer-dedizierten-platte-oder-einem-nas" tabindex="-1">Aufnahmen auf einer dedizierten Platte oder einem NAS <a class="header-anchor" href="#aufnahmen-auf-einer-dedizierten-platte-oder-einem-nas" aria-label="Permalink to &quot;Aufnahmen auf einer dedizierten Platte oder einem NAS&quot;">​</a></h2><p>Aufnahmen landen standardmäßig auf dem eigenen Volume des Containers. Für eine größere Platte, ein NAS-Share oder einen zusammengefassten Pool reichst du diesen Speicher in den Container und setzt den NVR-Speicherpfad darauf.</p><p>Unprivilegierte Container können NFS/CIFS-Shares nicht selbst mounten. Mounte das Share auf dem <strong>Proxmox-Host</strong> und reiche es als Mountpoint in den Container:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pct</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> set</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> &lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">CTI</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">D</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -mp0</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> /mnt/nas/recordings,mp=/mnt/recordings</span></span></code></pre></div><p>Setze dann den NVR-<strong>Speicherpfad</strong> auf <code>/mnt/recordings</code> (Einstellungen → Aufnahmen → Speicher), oder siehe den <a href="/de/install/docker#speicher-für-aufnahmen">Speicher-Abschnitt</a> der Docker-Seite.</p><div class="warning custom-block"><p class="custom-block-title">FUSE-Mounts (mergerfs, rclone) brauchen Sonderbehandlung</p><p>Ein FUSE-Mount (etwa ein <strong>mergerfs</strong>-Pool, der mehrere Platten zusammenfasst) propagiert <strong>nicht</strong> wie ein normales Dateisystem in einen (unprivilegierten) Container. Ist der FUSE-Mount nicht <strong>vor dem Container-Start</strong> auf dem Host aktiv, sieht der Container den leeren Mountpoint auf der Root-Disk des Hosts statt den Pool. Aufnahmen landen dann auf der kleinen OS-Platte und werden innerhalb von Minuten wieder rotiert, obwohl der Pool Terabytes frei hat.</p><p>Mounte den Pool auf dem Host <strong>vor</strong> dem Container-Start (z.B. per fstab-Eintrag oder systemd-Unit mit <code>Before=pve-container@&lt;CTID&gt;.service</code>) und stelle sicher, dass der Mount in den Container geteilt wird. Danach immer <strong>aus dem Container heraus verifizieren</strong>.</p></div><h2 id="speichergroße-verifizieren" tabindex="-1">Speichergröße verifizieren <a class="header-anchor" href="#speichergroße-verifizieren" aria-label="Permalink to &quot;Speichergröße verifizieren&quot;">​</a></h2><p>Nach dem Setzen eines eigenen Speicherpfads prüfen, ob der Container wirklich das volle Volume sieht. Dieser eine Check fängt fast jedes Mount-Problem ab:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pct</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> exec</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> &lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">CTI</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">D</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> df</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -h</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> /mnt/recordings</span></span></code></pre></div><p>Die angezeigte Größe muss zu deiner echten Platte oder deinem Pool passen. Zeigt sie stattdessen die kleine Container-Root-Größe, ist der Mount nicht propagiert. Dann die Schritte oben erneut prüfen. Der NVR loggt seinen aufgelösten Speicher samt Größe außerdem bei jedem Start und zeigt in <strong>Einstellungen → Aufnahmen</strong> eine Warnung, wenn das Volume unerwartet klein ist.</p><h2 id="verifizieren" tabindex="-1">Verifizieren <a class="header-anchor" href="#verifizieren" aria-label="Permalink to &quot;Verifizieren&quot;">​</a></h2><p>Bei jedem Start loggt der Container, welche Devices tatsächlich angekommen sind. Siehe <a href="/de/install/hardware-acceleration#aus-dem-container-verifizieren">Aus dem Container verifizieren</a>. Die camera.ui-UI ist unter <code>https://&lt;container-ip&gt;:3443</code> erreichbar (das Script gibt die IP am Ende aus; der Container taucht außerdem mit seinem Hostnamen in der Geräteliste deines Routers auf).</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("de/install/proxmox.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const proxmox = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  proxmox as default
};
