import { resolveComponent, useSSRContext } from "vue";
import { ssrRenderAttrs, ssrRenderStyle, ssrRenderComponent } from "vue/server-renderer";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Storage & retention","description":"","frontmatter":{"title":"Storage & retention"},"headers":[],"relativePath":"recording/storage.md","filePath":"recording/storage.md","lastUpdated":1784134587000}');
const _sfc_main = { name: "recording/storage.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  const _component_Shot = resolveComponent("Shot");
  _push(`<div${ssrRenderAttrs(_attrs)}><div style="${ssrRenderStyle({ "display": "none" })}" hidden="true" aria-hidden="true" data-nosnippet>Are you an LLM? You can read better optimized documentation at /recording/storage.md for this page in Markdown format</div><h1 id="storage-retention" tabindex="-1">Storage &amp; retention <a class="header-anchor" href="#storage-retention" aria-label="Permalink to &quot;Storage &amp; retention&quot;">​</a></h1><p>Recordings can use a lot of disk, so camera.ui gives you control over where they&#39;re stored and how long they&#39;re kept.</p><h2 id="where-recordings-are-stored" tabindex="-1">Where recordings are stored <a class="header-anchor" href="#where-recordings-are-stored" aria-label="Permalink to &quot;Where recordings are stored&quot;">​</a></h2><p>By default, recordings live with the rest of camera.ui&#39;s data. For more than a couple of cameras, put them on a <strong>dedicated local disk</strong>, not the system disk and not a network share. See <a href="/intro/requirements#storage-for-recordings">System requirements</a>.</p><h2 id="rough-sizing" tabindex="-1">Rough sizing <a class="header-anchor" href="#rough-sizing" aria-label="Permalink to &quot;Rough sizing&quot;">​</a></h2><p>Continuous recording writes the recorded stream to disk, so the size follows its bitrate. A rough guide:</p><p><strong>GB per day, per camera ≈ bitrate in Mbit/s × 10.8</strong></p><table tabindex="0"><thead><tr><th>Stream</th><th>Bitrate</th><th>Continuous, per day</th><th>Per camera, 7 days</th></tr></thead><tbody><tr><td>1080p H.264</td><td>4 Mbit/s</td><td>~43 GB</td><td>~300 GB</td></tr><tr><td>4K H.264</td><td>8 Mbit/s</td><td>~86 GB</td><td>~600 GB</td></tr><tr><td>H.265 (HEVC)</td><td>about half the bitrate</td><td>roughly half the above</td><td>roughly half the above</td></tr></tbody></table><p>Event recording only writes around motion or detections, so it uses a fraction of that, depending on how busy the scene is. When in doubt, start with a retention window or a storage cap and watch the per-camera rate (below) for a week.</p><h2 id="how-long-they-re-kept" tabindex="-1">How long they&#39;re kept <a class="header-anchor" href="#how-long-they-re-kept" aria-label="Permalink to &quot;How long they&#39;re kept&quot;">​</a></h2><p>Two limits decide when old footage is removed:</p><ul><li><strong>Retention (Days).</strong> Keep footage for up to this many days (0 means no day limit).</li><li><strong>Max Storage (GB).</strong> Cap the total space recordings may use (0 means no cap).</li></ul><p>When a limit is reached, what happens depends on the <strong>Retention Mode</strong>:</p><ul><li><strong>Overwrite</strong> (default). The oldest recordings are deleted to make room, so recording never stops.</li><li><strong>Strict.</strong> Nothing is deleted beyond the retention window. If the disk fills, recording pauses until you free space.</li></ul><h2 id="footage-from-removed-cameras" tabindex="-1">Footage from removed cameras <a class="header-anchor" href="#footage-from-removed-cameras" aria-label="Permalink to &quot;Footage from removed cameras&quot;">​</a></h2><p>If you delete a camera, or unassign the NVR from it, its recordings stay on disk. They still count towards <strong>Max Storage</strong>, and retention and cleanup still remove them when the disk gets tight, like an active camera&#39;s recordings.</p><p>To keep that footage instead, put an empty file named <code>.cameraui-keep</code> into the camera&#39;s folder in your recordings directory. camera.ui then leaves the folder alone: not counted, not aged out, not touched when space runs low.<sup class="footnote-ref"><a href="#fn1" id="fnref1">[1]</a></sup></p><h2 id="keeping-an-eye-on-usage" tabindex="-1">Keeping an eye on usage <a class="header-anchor" href="#keeping-an-eye-on-usage" aria-label="Permalink to &quot;Keeping an eye on usage&quot;">​</a></h2>`);
  _push(ssrRenderComponent(_component_Shot, {
    src: "/img/recording/storage-stats.png",
    alt: "Storage usage statistics"
  }, null, _parent));
  _push(`<p>camera.ui shows how much space is used and free, and a breakdown per camera, including each camera&#39;s recording rate (megabytes per hour) and how many days of footage it holds. This helps you size storage and spot a camera recording more than expected.</p><p>If the page warns that the storage volume is small, most of the volume is kept free as headroom, so recordings rotate out quickly. That usually means the larger disk you meant to use isn&#39;t mounted, so check the storage path. A separate warning appears when free disk space drops below 8%. Below 5%, recording pauses.</p><h2 id="uninstalling-the-nvr" tabindex="-1">Uninstalling the NVR <a class="header-anchor" href="#uninstalling-the-nvr" aria-label="Permalink to &quot;Uninstalling the NVR&quot;">​</a></h2><p>Uninstalling the NVR plugin with <strong>Also delete stored data</strong> removes its settings, databases and caches, but leaves your recordings folder in place. The folder is protected by a <code>.cameraui-keep</code> file inside it. If the recordings should go with the plugin, delete that file first, then uninstall.</p><hr class="footnotes-sep"><section class="footnotes"><ol class="footnotes-list"><li id="fn1" class="footnote-item"><p>The marker only applies to cameras camera.ui no longer knows. The folder of an active camera follows retention and the storage cap either way. <a href="#fnref1" class="footnote-backref">↩︎</a></p></li></ol></section></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("recording/storage.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const storage = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  storage as default
};
