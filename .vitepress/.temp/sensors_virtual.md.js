import { resolveComponent, useSSRContext } from "vue";
import { ssrRenderAttrs, ssrRenderStyle, ssrRenderComponent } from "vue/server-renderer";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Virtual sensors","description":"","frontmatter":{"title":"Virtual sensors"},"headers":[],"relativePath":"sensors/virtual.md","filePath":"sensors/virtual.md","lastUpdated":1785427198000}');
const _sfc_main = { name: "sensors/virtual.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  const _component_Shot = resolveComponent("Shot");
  _push(`<div${ssrRenderAttrs(_attrs)}><div style="${ssrRenderStyle({ "display": "none" })}" hidden="true" aria-hidden="true" data-nosnippet>Are you an LLM? You can read better optimized documentation at /sensors/virtual.md for this page in Markdown format</div><h1 id="virtual-sensors" tabindex="-1">Virtual sensors <a class="header-anchor" href="#virtual-sensors" aria-label="Permalink to &quot;Virtual sensors&quot;">​</a></h1><p>A <strong>virtual sensor</strong> is a sensor with no plugin behind it. You create it yourself, camera.ui holds its state, and you set that state from the UI or from an <a href="/automations/">automation</a>.</p><p>Use one to mirror an external device or a state into camera.ui: a doorbell you trigger from an automation, a contact you flip when a message arrives over MQTT, a switch that stands for something no plugin reports. Once created, a virtual sensor behaves like any other <a href="/sensors/">sensor</a>. You can assign it to cameras, place it on the video as a <a href="/cameras/shortcuts">shortcut</a>, export it to your smart home, and use it as an automation trigger.</p><p>Creating, renaming, and deleting virtual sensors needs admin.</p><h2 id="create-a-virtual-sensor" tabindex="-1">Create a virtual sensor <a class="header-anchor" href="#create-a-virtual-sensor" aria-label="Permalink to &quot;Create a virtual sensor&quot;">​</a></h2><p>Open the <strong>Sensors</strong> page and click <strong>Create virtual sensor</strong>. Fill in:</p><ul><li><strong>Type.</strong> What the sensor represents. Pick it once, it can&#39;t be changed later.</li><li><strong>Name.</strong> How the sensor appears in camera.ui, for example &quot;Front Door&quot;. Two virtual sensors of the same type can&#39;t share a name.</li></ul><p>The sensor starts in its idle state: off, closed, locked, or disarmed, depending on the type. It belongs to no camera yet; click its row to assign cameras and it shows up in their Overview right away.<sup class="footnote-ref"><a href="#fn1" id="fnref1">[1]</a></sup></p>`);
  _push(ssrRenderComponent(_component_Shot, {
    src: "/img/sensors/virtual-sensors-list.png",
    alt: "Virtual sensors on the Sensors page"
  }, null, _parent));
  _push(`<h2 id="available-types" tabindex="-1">Available types <a class="header-anchor" href="#available-types" aria-label="Permalink to &quot;Available types&quot;">​</a></h2><p>There are 13 types:</p><ul><li><strong>Doorbell.</strong> A ring you trigger. It resets itself two seconds later.</li><li><strong>Switch.</strong> On or off.</li><li><strong>Contact.</strong> Open or closed, for a door or window.</li><li><strong>Occupancy.</strong> Occupied or empty.</li><li><strong>Smoke.</strong> Clear or triggered.</li><li><strong>Leak.</strong> Clear or triggered.</li><li><strong>Light.</strong> On or off, with brightness.</li><li><strong>Siren.</strong> On or off, with volume.</li><li><strong>Lock.</strong> Locked or unlocked.</li><li><strong>Garage.</strong> Open or closed.</li><li><strong>Security System.</strong> Off, Home, Away, or Night.</li><li><strong>Temperature.</strong> A temperature reading.</li><li><strong>Humidity.</strong> A humidity reading.</li></ul><h2 id="control-it" tabindex="-1">Control it <a class="header-anchor" href="#control-it" aria-label="Permalink to &quot;Control it&quot;">​</a></h2><p>On an assigned camera&#39;s <strong>Overview</strong> tab, Doorbell, Switch, Light, Siren, Lock, Garage, and Security System sit there with their controls and work the same as the plugin-provided ones, see <a href="/sensors/controls">Controls &amp; states</a>. The same controls appear in the sensor&#39;s edit dialog on the <strong>Sensors</strong> page, which also covers sensors assigned to no camera.</p><p>Contact, Occupancy, Smoke, Leak, Temperature, and Humidity are read-only. Their value comes from an automation.</p><p>Lock, Garage, and Security System follow what you set immediately. There is no hardware to report back, so they never sit in a &quot;locking&quot; or &quot;opening&quot; state.</p><p>camera.ui remembers the last state of a virtual sensor, so it survives a restart.</p><h2 id="control-it-from-an-automation" tabindex="-1">Control it from an automation <a class="header-anchor" href="#control-it-from-an-automation" aria-label="Permalink to &quot;Control it from an automation&quot;">​</a></h2><p>In the <a href="/automations/">automation editor</a>, a <strong>Control sensor</strong> action sets a virtual sensor, and a <strong>Sensor change</strong> trigger reacts when its state changes. For the read-only types this is the only way to set a value: an automation that receives an MQTT message, for example, can put that value on a virtual temperature sensor.</p><h2 id="rename-or-delete" tabindex="-1">Rename or delete <a class="header-anchor" href="#rename-or-delete" aria-label="Permalink to &quot;Rename or delete&quot;">​</a></h2><p>Both happen on the <strong>Sensors</strong> page: click the row to rename the sensor, use the delete button on the row to remove it.<sup class="footnote-ref"><a href="#fn2" id="fnref2">[2]</a></sup></p><p>Renaming changes the name everywhere, in the Overview, on shortcuts, and in automations. Automations that use the sensor keep working.</p><hr class="footnotes-sep"><section class="footnotes"><ol class="footnotes-list"><li id="fn1" class="footnote-item"><p>Deleting a camera only removes the assignment. The sensor itself stays and can be assigned to other cameras. <a href="#fnref1" class="footnote-backref">↩︎</a></p></li><li id="fn2" class="footnote-item"><p>Deleting a virtual sensor stops any automation that references it. Creating a new sensor with the same type and name does not restore the link, since it is a new sensor. Point the automation at it again. <a href="#fnref2" class="footnote-backref">↩︎</a></p></li></ol></section></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("sensors/virtual.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const virtual = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  virtual as default
};
