import { resolveComponent, useSSRContext } from "vue";
import { ssrRenderAttrs, ssrRenderStyle, ssrRenderComponent } from "vue/server-renderer";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Updating","description":"","frontmatter":{},"headers":[],"relativePath":"install/updating.md","filePath":"install/updating.md","lastUpdated":1784134587000}');
const _sfc_main = { name: "install/updating.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  const _component_Shot = resolveComponent("Shot");
  _push(`<div${ssrRenderAttrs(_attrs)}><div style="${ssrRenderStyle({ "display": "none" })}" hidden="true" aria-hidden="true" data-nosnippet>Are you an LLM? You can read better optimized documentation at /install/updating.md for this page in Markdown format</div><h1 id="updating" tabindex="-1">Updating <a class="header-anchor" href="#updating" aria-label="Permalink to &quot;Updating&quot;">​</a></h1><p>camera.ui has two parts that update: the <strong>server</strong> (the core that does the work) and the <strong>app</strong> you installed it with. Most updates are automatic or a single click. You can check the current versions and run updates from <strong>Settings → <a href="/admin/system">System</a></strong>.</p><h2 id="updating-the-server" tabindex="-1">Updating the server <a class="header-anchor" href="#updating-the-server" aria-label="Permalink to &quot;Updating the server&quot;">​</a></h2><p>The server is the most important part to keep current. How you update it depends on how you installed it:</p><ul><li><strong>Desktop app.</strong> The app updates the server for you automatically, before it starts. There is nothing to do.</li><li><strong>Docker or bare-metal.</strong> When a new server version is available, <strong>Settings → <a href="/admin/system">System</a></strong> shows an <strong>Update</strong> button. Select it, and the server installs the update and restarts.</li></ul><h2 id="beta-updates" tabindex="-1">Beta updates <a class="header-anchor" href="#beta-updates" aria-label="Permalink to &quot;Beta updates&quot;">​</a></h2><p>To try pre-release versions early, turn on <strong>Beta updates</strong> in <strong>Settings → <a href="/admin/system">System</a></strong>. The server then offers pre-release versions, and <strong>Manage</strong> lists them so you can pick a specific one. On the mobile app the same switch also turns on beta over-the-air updates of the interface. Pre-release builds can be less stable, so use this only if you want to help test.<sup class="footnote-ref"><a href="#fn1" id="fnref1">[1]</a></sup></p><h2 id="updating-the-desktop-app" tabindex="-1">Updating the desktop app <a class="header-anchor" href="#updating-the-desktop-app" aria-label="Permalink to &quot;Updating the desktop app&quot;">​</a></h2><p>The <a href="/install/desktop">desktop app</a> keeps itself up to date. It installs updates on its own at startup. While the app is running it keeps checking, and when a new version is found it tells you and shows an <strong>Update</strong> button in <strong>Settings → <a href="/admin/system">System</a></strong>. The update installs when you select it, or on the next start. Because Server mode also updates the bundled server, the app and the server stay in sync.</p><p>One exception on Linux: only the <code>.AppImage</code> updates itself. If you installed the <code>.deb</code>, update by downloading and installing the new <code>.deb</code> from the <a href="https://cameraui.com" target="_blank" rel="noreferrer">download page</a>.</p><h2 id="updating-the-docker-image" tabindex="-1">Updating the Docker image <a class="header-anchor" href="#updating-the-docker-image" aria-label="Permalink to &quot;Updating the Docker image&quot;">​</a></h2><p>Updating the server from the UI does not change the <a href="/install/docker">Docker</a> image. To update the image itself (the base OS, GPU libraries, and launcher), pull the latest and recreate the container:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">docker</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> compose</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> pull</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">docker</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> compose</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> up</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -d</span></span></code></pre></div><p>The server version you chose is stored separately and persists across image pulls, so an image update never resets it.</p><h2 id="updating-the-mobile-apps" tabindex="-1">Updating the mobile apps <a class="header-anchor" href="#updating-the-mobile-apps" aria-label="Permalink to &quot;Updating the mobile apps&quot;">​</a></h2><p>The <a href="/install/mobile">mobile apps</a> update through the App Store and Google Play like any other app (during the beta, through TestFlight on iOS and the Play Store testing track on Android), and also receive over-the-air updates to the interface.</p>`);
  _push(ssrRenderComponent(_component_Shot, {
    src: "/img/install/system-update.png",
    alt: "Settings then System update button"
  }, null, _parent));
  _push(`<hr class="footnotes-sep"><section class="footnotes"><ol class="footnotes-list"><li id="fn1" class="footnote-item"><p>The desktop app does not show the switch, because it updates the server together with the app. <a href="#fnref1" class="footnote-backref">↩︎</a></p></li></ol></section></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("install/updating.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const updating = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  updating as default
};
