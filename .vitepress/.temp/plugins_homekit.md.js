import { resolveComponent, useSSRContext } from "vue";
import { ssrRenderAttrs, ssrRenderStyle, ssrRenderComponent } from "vue/server-renderer";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"HomeKit","description":"","frontmatter":{"title":"HomeKit"},"headers":[],"relativePath":"plugins/homekit.md","filePath":"plugins/homekit.md","lastUpdated":1785427198000}');
const _sfc_main = { name: "plugins/homekit.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  const _component_Shot = resolveComponent("Shot");
  _push(`<div${ssrRenderAttrs(_attrs)}><div style="${ssrRenderStyle({ "display": "none" })}" hidden="true" aria-hidden="true" data-nosnippet>Are you an LLM? You can read better optimized documentation at /plugins/homekit.md for this page in Markdown format</div><h1 id="homekit" tabindex="-1">HomeKit <a class="header-anchor" href="#homekit" aria-label="Permalink to &quot;HomeKit&quot;">​</a></h1><p>The HomeKit plugin adds your cameras and sensors to <strong>Apple Home</strong>. Cameras stream and record through the Home app across your Apple devices; sensors appear as accessories you can see and control there.</p>`);
  _push(ssrRenderComponent(_component_Shot, {
    src: "/img/plugins/homekit-pairing.png",
    alt: "HomeKit camera settings with pairing QR code"
  }, null, _parent));
  _push(`<h2 id="setup" tabindex="-1">Setup <a class="header-anchor" href="#setup" aria-label="Permalink to &quot;Setup&quot;">​</a></h2><ol><li>On the <strong>Plugins</strong> page, search for and install the <strong>HomeKit</strong> plugin, then enable it.</li><li>Assign it to the cameras you want in Home, on the plugin&#39;s <strong>Cameras</strong> tab or from a <a href="/sensors/setup">camera&#39;s Plugins tab</a>.</li><li>Open a camera&#39;s <strong>HomeKit settings</strong>. Each camera is added to Home individually and has its own <strong>QR code</strong> and <strong>PIN</strong>.</li><li>In the Apple <strong>Home</strong> app, choose <strong>Add Accessory</strong> and scan the camera&#39;s QR code, or enter its PIN. Repeat for each camera.</li></ol><p>Once added, the camera streams in Home and supports <strong>HomeKit Secure Video</strong> recording if you&#39;ve set that up in Home.</p><h2 id="what-ends-up-where" tabindex="-1">What ends up where <a class="header-anchor" href="#what-ends-up-where" aria-label="Permalink to &quot;What ends up where&quot;">​</a></h2><p>A camera comes across as one accessory with everything that belongs to it: its motion sensor, doorbell button, spotlight, siren, and battery.</p><p>Every other <a href="/sensors/">exposed sensor</a> (contact, occupancy, smoke, leak, temperature, humidity, lock, garage, switch, security system, standalone lights and sirens) lives behind a single <strong>camera.ui Bridge</strong> accessory. The bridge appears as soon as the first such sensor is exposed; pair it once and every sensor you expose later joins automatically. Its QR code and PIN sit in the plugin&#39;s settings.</p><p>Whether a sensor comes across at all is the <strong>Expose sensor</strong> toggle on the <a href="/sensors/setup#the-sensors-page">Sensors page</a>.</p><h2 id="fast-streaming" tabindex="-1">Fast streaming <a class="header-anchor" href="#fast-streaming" aria-label="Permalink to &quot;Fast streaming&quot;">​</a></h2><p>Opening a camera in Home is near-instant, taking only a few milliseconds, whether the camera uses H.264 or H.265. This relies on the source being kept ready, so enable <a href="/cameras/add-camera#source-options">hot mode and preload</a> on the cameras you bridge.</p><h2 id="settings" tabindex="-1">Settings <a class="header-anchor" href="#settings" aria-label="Permalink to &quot;Settings&quot;">​</a></h2><p>Each camera&#39;s HomeKit settings include:</p><ul><li><strong>QR code and PIN.</strong> The pairing details for that camera.</li><li><strong>Hardware acceleration.</strong> Uses the GPU, if one is available, for HomeKit Secure Video recordings. On by default.</li><li><strong>Adaptive stream source.</strong> On by default. When you&#39;re away from home on a slow connection, camera.ui picks the source that matches the size Home asks for: high, mid, or low resolution. Turn it off to always use the primary source.</li><li><strong>Port and mDNS advertiser.</strong> Advanced networking options; the defaults are usually fine.</li><li><strong>Reset pairing.</strong> Generate a new code and re-add the camera if pairing breaks.</li></ul><p>The plugin&#39;s own settings hold the same set for the sensor bridge: its QR code, PIN, port, mDNS advertiser, and a <strong>Reset Bridge Pairing</strong> button.</p><h2 id="good-to-know" tabindex="-1">Good to know <a class="header-anchor" href="#good-to-know" aria-label="Permalink to &quot;Good to know&quot;">​</a></h2><ul><li>Each camera is its own HomeKit accessory, so you add cameras one at a time. Standalone sensors share the one bridge and only need a single pairing.</li><li>Cameras that support two-way audio can be talked to from the Home app.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("plugins/homekit.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const homekit = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  homekit as default
};
