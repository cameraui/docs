import { resolveComponent, useSSRContext } from "vue";
import { ssrRenderAttrs, ssrRenderStyle, ssrRenderComponent } from "vue/server-renderer";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"MQTT","description":"","frontmatter":{"title":"MQTT"},"headers":[],"relativePath":"admin/mqtt.md","filePath":"admin/mqtt.md","lastUpdated":1785427198000}');
const _sfc_main = { name: "admin/mqtt.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  const _component_Shot = resolveComponent("Shot");
  _push(`<div${ssrRenderAttrs(_attrs)}><div style="${ssrRenderStyle({ "display": "none" })}" hidden="true" aria-hidden="true" data-nosnippet>Are you an LLM? You can read better optimized documentation at /admin/mqtt.md for this page in Markdown format</div><h1 id="mqtt" tabindex="-1">MQTT <a class="header-anchor" href="#mqtt" aria-label="Permalink to &quot;MQTT&quot;">​</a></h1><p>The <strong>Settings → MQTT</strong> page (admins only) connects camera.ui to an MQTT broker. Once connected, camera.ui publishes server events, camera states, detections, snapshots and sensor values as MQTT topics, and accepts commands for your sensors. It&#39;s off by default.</p>`);
  _push(ssrRenderComponent(_component_Shot, {
    src: "/img/admin/mqtt.png",
    alt: "The MQTT settings page"
  }, null, _parent));
  _push(`<h2 id="turning-it-on" tabindex="-1">Turning it on <a class="header-anchor" href="#turning-it-on" aria-label="Permalink to &quot;Turning it on&quot;">​</a></h2><p>Switch <strong>Enabled</strong> on, pick a mode, fill in the connection, and <strong>Save</strong>. Every save restarts the connection, so a wrong value shows up right away in the status card at the top of the page.</p><h2 id="mode" tabindex="-1">Mode <a class="header-anchor" href="#mode" aria-label="Permalink to &quot;Mode&quot;">​</a></h2><p><strong>Mode</strong> decides where the broker lives.</p><ul><li><strong>External broker.</strong> camera.ui connects to a broker you already run (Mosquitto, the Home Assistant Mosquitto add-on, and so on).</li><li><strong>Built-in broker.</strong> camera.ui runs its own broker and connects to it locally.</li></ul><h3 id="built-in-broker" tabindex="-1">Built-in broker <a class="header-anchor" href="#built-in-broker" aria-label="Permalink to &quot;Built-in broker&quot;">​</a></h3><p>There&#39;s nothing to install. camera.ui starts the broker on <strong>Port</strong> (default <code>1883</code>) and shows the <strong>Broker address</strong> as a read-only field with a copy button, in the form <code>mqtt://&lt;server host&gt;:&lt;port&gt;</code>. Point your MQTT devices at that address.</p><p>The credentials next to it are what those devices need:</p><ul><li><strong>Username.</strong> Defaults to <code>cameraui</code>.</li><li><strong>Password.</strong> Generated randomly on first start. It has its own copy button, and you can replace it with your own.<sup class="footnote-ref"><a href="#fn1" id="fnref1">[1]</a></sup></li></ul><h3 id="external-broker" tabindex="-1">External broker <a class="header-anchor" href="#external-broker" aria-label="Permalink to &quot;External broker&quot;">​</a></h3><ul><li><strong>Host.</strong> The broker&#39;s hostname or IP, for example <code>192.168.1.10</code>.</li><li><strong>Port.</strong> Defaults to <code>1883</code>.</li><li><strong>Protocol.</strong> <code>mqtt:// (TCP)</code> or <code>mqtts:// (TLS)</code>. Default is <code>mqtt://</code>.</li><li><strong>Username</strong> and <strong>Password.</strong> Leave both empty for an anonymous broker. The password field stays masked after saving. Leave it empty to keep the stored one, type a new one to replace it.</li></ul><h2 id="client-id-and-topic-prefix" tabindex="-1">Client ID and topic prefix <a class="header-anchor" href="#client-id-and-topic-prefix" aria-label="Permalink to &quot;Client ID and topic prefix&quot;">​</a></h2><p>Both apply to either mode.</p><ul><li><strong>Client ID.</strong> The name camera.ui registers with at the broker. Defaults to <code>cameraui</code>. Change it if something else on your network already uses that ID.</li><li><strong>Topic prefix.</strong> Everything camera.ui publishes lives below this prefix. Defaults to <code>cameraui</code>. Letters, numbers, <code>_</code>, <code>-</code> and <code>/</code> separators only.</li></ul><h2 id="tls" tabindex="-1">TLS <a class="header-anchor" href="#tls" aria-label="Permalink to &quot;TLS&quot;">​</a></h2><p>The TLS fields appear for an external broker on <code>mqtts://</code>.</p><ul><li><strong>Verify server certificate.</strong> On by default. Turn it off only for a broker with a self-signed certificate.</li><li><strong>CA certificate (PEM).</strong> The certificate authority that signed the broker&#39;s certificate.</li><li><strong>Client certificate (PEM)</strong> and <strong>Client key (PEM).</strong> For brokers that require client certificates.</li></ul><h2 id="testing-and-status" tabindex="-1">Testing and status <a class="header-anchor" href="#testing-and-status" aria-label="Permalink to &quot;Testing and status&quot;">​</a></h2><p><strong>Test connection</strong> opens a short-lived, separate connection with the values currently in the form and reports back as a toast. It doesn&#39;t save anything. For the built-in broker, save first: the test connects to the running broker, so it fails while the broker isn&#39;t up yet.<sup class="footnote-ref"><a href="#fn2" id="fnref2">[2]</a></sup></p><p>The status card at the top shows the live state of the connection:</p><table tabindex="0"><thead><tr><th>State</th><th>Meaning</th></tr></thead><tbody><tr><td>Disabled</td><td>MQTT is off, or an external broker has no host set</td></tr><tr><td>Connecting…</td><td>The connection is being established</td></tr><tr><td>Connected</td><td>Connected to the broker</td></tr><tr><td>Reconnecting…</td><td>The connection dropped, camera.ui is retrying</td></tr><tr><td>Error</td><td>The last attempt failed · the card shows the broker&#39;s message</td></tr></tbody></table><p>When the built-in broker is running, the card says so along with its port.</p><h2 id="topic-reference" tabindex="-1">Topic reference <a class="header-anchor" href="#topic-reference" aria-label="Permalink to &quot;Topic reference&quot;">​</a></h2><p><code>&lt;prefix&gt;</code> below is your topic prefix, <code>&lt;id&gt;</code> the camera&#39;s ID, <code>&lt;sensorId&gt;</code> a sensor&#39;s ID. In topic segments, <code>#</code>, <code>+</code>, <code>/</code> and whitespace are replaced with <code>-</code>.</p><p>State topics are retained, so a subscriber that connects later still gets the current value. Events are not retained.</p><p>Sensors publish under their own tree, not under a camera: a <a href="/sensors/">sensor</a> is its own entity and can belong to several cameras or none. Only <a href="/sensors/">exposed</a> sensors are published; turning the expose toggle off clears the sensor&#39;s retained topics.</p><h3 id="what-camera-ui-publishes" tabindex="-1">What camera.ui publishes <a class="header-anchor" href="#what-camera-ui-publishes" aria-label="Permalink to &quot;What camera.ui publishes&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Topic</th><th>Payload</th><th>Retained</th></tr></thead><tbody><tr><td><code>&lt;prefix&gt;/status</code></td><td><code>online</code> · <code>offline</code></td><td>yes</td></tr><tr><td><code>&lt;prefix&gt;/server/state</code></td><td>JSON with the server version and a timestamp</td><td>yes</td></tr><tr><td><code>&lt;prefix&gt;/server/event</code></td><td>JSON, <code>started</code> or <code>shutdown</code> with a timestamp</td><td>no</td></tr><tr><td><code>&lt;prefix&gt;/server/notification</code></td><td>JSON, system notifications</td><td>no</td></tr><tr><td><code>&lt;prefix&gt;/plugin/&lt;name&gt;/status</code></td><td><code>started</code> · <code>stopped</code> · <code>error</code> · <code>crashed</code></td><td>yes</td></tr><tr><td><code>&lt;prefix&gt;/camera/&lt;id&gt;/status</code></td><td><code>online</code> · <code>offline</code></td><td>yes</td></tr><tr><td><code>&lt;prefix&gt;/camera/&lt;id&gt;/frameworker</code></td><td><code>online</code> · <code>offline</code></td><td>yes</td></tr><tr><td><code>&lt;prefix&gt;/camera/&lt;id&gt;/meta</code></td><td>JSON: id, name, room, type, disabled, camera info</td><td>yes</td></tr><tr><td><code>&lt;prefix&gt;/camera/&lt;id&gt;/event</code></td><td>JSON detection event, start to end</td><td>no</td></tr><tr><td><code>&lt;prefix&gt;/camera/&lt;id&gt;/motion</code></td><td><code>ON</code> · <code>OFF</code></td><td>yes</td></tr><tr><td><code>&lt;prefix&gt;/camera/&lt;id&gt;/detection/&lt;label&gt;</code></td><td><code>ON</code> · <code>OFF</code>, one topic per detected label</td><td>yes</td></tr><tr><td><code>&lt;prefix&gt;/camera/&lt;id&gt;/snapshot</code></td><td>JPEG of the last event thumbnail</td><td>yes</td></tr><tr><td><code>&lt;prefix&gt;/sensor/&lt;sensorId&gt;/meta</code></td><td>JSON: ID, type, name, display name, plugin, assigned cameras</td><td>yes</td></tr><tr><td><code>&lt;prefix&gt;/sensor/&lt;sensorId&gt;/&lt;property&gt;</code></td><td>the property&#39;s value as JSON</td><td>yes</td></tr></tbody></table><p><code>&lt;prefix&gt;/status</code> is also the last will: if camera.ui goes away without saying goodbye, the broker publishes <code>offline</code> for it.</p><p>The motion topic goes <code>ON</code> when an event includes motion, a label topic when that label is first detected. Both go back to <code>OFF</code> when the event ends. The event payload carries the detection details without any image data. The image goes to the snapshot topic.</p><h3 id="what-camera-ui-subscribes-to" tabindex="-1">What camera.ui subscribes to <a class="header-anchor" href="#what-camera-ui-subscribes-to" aria-label="Permalink to &quot;What camera.ui subscribes to&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Topic</th><th>Purpose</th></tr></thead><tbody><tr><td><code>&lt;prefix&gt;/sensor/&lt;sensorId&gt;/&lt;property&gt;/set</code></td><td>Set a sensor property</td></tr></tbody></table><p>Publish to a <code>/set</code> topic to control a sensor: <code>ON</code> and <code>OFF</code> become true and false, anything else is read as JSON and falls back to plain text. Detection sensors ignore commands, their values come from the camera.</p><p>Automations with an MQTT trigger subscribe to their own topics on top of this.</p><h2 id="home-assistant-discovery" tabindex="-1">Home Assistant discovery <a class="header-anchor" href="#home-assistant-discovery" aria-label="Permalink to &quot;Home Assistant discovery&quot;">​</a></h2><p><strong>Home Assistant discovery</strong> makes Home Assistant create the devices and entities for you, no YAML. It&#39;s off by default.</p><div class="warning custom-block"><p class="custom-block-title">Pick one path, not both</p><p>MQTT discovery is one of two ways to get camera.ui entities into Home Assistant. The other is the <a href="/home-assistant/integration">Home Assistant integration</a>, which needs no broker. The two do not merge: run both and every device shows up twice. Use MQTT discovery if you already run a broker, otherwise use the integration.</p></div><p>Turn it on and check the <strong>Discovery prefix</strong>. It has to match the discovery prefix configured in Home Assistant, which is <code>homeassistant</code> unless you changed it.</p><p>Home Assistant then builds one device per camera, using the camera&#39;s name, plus its model and firmware version when camera.ui knows them. Each device gets:</p><ul><li><strong>Status.</strong> A connectivity sensor, listed under diagnostics.</li><li><strong>Motion.</strong> A motion sensor.</li><li><strong>Snapshot.</strong> A camera entity fed by the snapshot topic.</li><li><strong>Person, Vehicle, Animal, Package.</strong> Detection sensors, only for cameras that have object detection.</li></ul><p>Every <a href="/sensors/">exposed sensor</a> becomes a device of its own when its type maps to a Home Assistant one: contact, occupancy, smoke and leak sensors, doorbells, temperature, humidity and battery readings, switches, lights (with brightness where the light supports it), sirens, locks, garage doors and security systems. A sensor assigned to exactly one camera is linked below that camera&#39;s device. Switches, lights, sirens, locks, garage doors and security systems are controllable from Home Assistant.<sup class="footnote-ref"><a href="#fn3" id="fnref3">[3]</a></sup></p><p>Entities stay available only while both camera.ui and the camera are online. Status is the exception: it follows camera.ui alone, so it can still report the camera as offline. Turning discovery back off, turning MQTT off, or changing the discovery prefix or topic prefix removes the entities camera.ui created.</p><h2 id="next-steps" tabindex="-1">Next steps <a class="header-anchor" href="#next-steps" aria-label="Permalink to &quot;Next steps&quot;">​</a></h2><ul><li><strong><a href="/automations/">Automations</a></strong> — react to an incoming MQTT message, or publish one as an action.</li><li><strong><a href="/sensors/">Sensors</a></strong> — the sensors that show up on the sensor topics and in Home Assistant, and the expose toggle that controls it.</li></ul><hr class="footnotes-sep"><section class="footnotes"><ol class="footnotes-list"><li id="fn1" class="footnote-item"><p>Clearing the username or the password lets the built-in broker accept any client without credentials. <a href="#fnref1" class="footnote-backref">↩︎</a></p></li><li id="fn2" class="footnote-item"><p>Testing the built-in broker before it&#39;s ever been saved reports that the broker isn&#39;t running. <a href="#fnref2" class="footnote-backref">↩︎</a></p></li><li id="fn3" class="footnote-item"><p>Detection sensors (motion, object, audio, face, license plate and the like) get no entity of their own. They feed the Motion and detection entities above. <a href="#fnref3" class="footnote-backref">↩︎</a></p></li></ol></section></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("admin/mqtt.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const mqtt = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  mqtt as default
};
