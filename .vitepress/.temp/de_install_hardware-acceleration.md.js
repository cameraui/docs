import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Hardware-Beschleunigung","description":"","frontmatter":{"title":"Hardware-Beschleunigung"},"headers":[],"relativePath":"de/install/hardware-acceleration.md","filePath":"de/install/hardware-acceleration.md","lastUpdated":1784589260000}');
const _sfc_main = { name: "de/install/hardware-acceleration.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><div style="${ssrRenderStyle({ "display": "none" })}" hidden="true" aria-hidden="true" data-nosnippet>Are you an LLM? You can read better optimized documentation at /de/install/hardware-acceleration.md for this page in Markdown format</div><h1 id="hardware-beschleunigung" tabindex="-1">Hardware-Beschleunigung <a class="header-anchor" href="#hardware-beschleunigung" aria-label="Permalink to &quot;Hardware-Beschleunigung&quot;">​</a></h1><p>camera.ui beschleunigt zwei Dinge: die <strong>Video-Pipeline</strong> (Dekodieren der Kamera-Streams für Erkennung, Live-Ansicht und Snapshots sowie Enkodieren fürs Streaming) und die <strong>KI-Inferenz</strong> (Objekterkennung und alles, was darauf aufbaut).</p><p><strong>Video-Beschleunigung ist auf jeder Plattform automatisch.</strong> camera.ui prüft die Hardware zur Laufzeit, wählt das beste verfügbare Backend und fällt auf Software zurück, wenn keins da ist, sodass es nichts zu konfigurieren gibt. Auch KI-Beschleuniger arbeiten automatisch, sobald die Hardware erreichbar ist; unter Docker heißt das: das Device einmal durchreichen (siehe unten).</p><h2 id="was-lauft-wo" tabindex="-1">Was läuft wo <a class="header-anchor" href="#was-lauft-wo" aria-label="Permalink to &quot;Was läuft wo&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Plattform</th><th>Video-Pipeline (automatisch)</th><th>KI-Backends</th></tr></thead><tbody><tr><td><strong>macOS</strong> (Desktop-App, Intel &amp; Apple Silicon)</td><td>VideoToolbox (Apple GPU + Media Engine)</td><td>CoreML (Apple GPU + <strong>Neural Engine</strong>), ONNX</td></tr><tr><td><strong>Windows</strong> (Desktop-App, x64)</td><td>NVIDIA (CUDA/NVENC), Intel (QSV) und jede GPU via Direct3D 11/12</td><td>OpenVINO (Intel GPU/NPU), ONNX (NVIDIA CUDA)</td></tr><tr><td><strong>Linux x64</strong> (Docker, bare-metal, Proxmox)</td><td>NVIDIA (CUDA/NVENC), Intel (VA-API/QSV), AMD (VA-API), dazu Vulkan/OpenCL</td><td>OpenVINO (Intel GPU/NPU), ONNX (CUDA/TensorRT), NCNN (Vulkan), Coral, Hailo</td></tr><tr><td><strong>Linux arm64</strong> (Rockchip)</td><td>Rockchip (RKMPP)</td><td>Coral, Hailo, NCNN (CPU/NEON)</td></tr></tbody></table><p>Die Tabelle zeigt, was camera.ui nutzen <strong>kann</strong>. Der passende GPU-Treiber muss trotzdem auf dem Host installiert sein. camera.ui probiert beim Start jedes Backend aus und wählt nur, was wirklich funktioniert; alles, dessen Treiber fehlt, wird still übersprungen und erzeugt nie einen Fehler.</p><p>Auf der <strong>Desktop-App</strong> gibt es nichts einzurichten: macOS bringt die Video-Beschleunigung als Teil des Betriebssystems mit, und unter Windows kommt der Grafiktreiber mit dem System. Die Hardware wird automatisch erkannt. Jedes Erkennungs-Plugin loggt, worauf jedes Modell läuft (<code>Loaded model: …</code>), und zeigt es unter <strong>Active Hardware</strong> in seinen Plugin-Einstellungen.</p><p>Der Rest dieser Seite ist für <strong>Linux und Docker</strong>, wo Hardware die Container-Grenze überwinden muss. Das sind immer dieselben drei Schritte:</p><ol><li><strong>Host-Treiber.</strong> Der kernelseitige Treiber muss auf dem Host installiert sein (der einzige Teil, den ein Docker-Image nie mitliefern kann).</li><li><strong>Device-Passthrough.</strong> Der Device-Node kommt in den <code>devices:</code>-Abschnitt deiner Compose-Datei.</li><li><strong>Verifizieren.</strong> Der Container sagt dir, was tatsächlich angekommen ist.</li></ol><p>GPUs wählst du über den passenden <a href="/de/install/docker#hardware-beschleunigung">Image-Flavor</a>. KI-Beschleuniger funktionieren mit <strong>jedem</strong> Flavor: Die Coral-Runtime steckt in jedem Image, und die Hailo-Runtime bringt ihr Plugin selbst mit. Die einzige Ausnahme ist die Intel NPU, deren Userspace-Treiber nur im <strong>intel-Flavor</strong> enthalten ist.</p><h2 id="host-prufen" tabindex="-1">Host prüfen <a class="header-anchor" href="#host-prufen" aria-label="Permalink to &quot;Host prüfen&quot;">​</a></h2><p>Das <a href="https://github.com/cameraui/docker" target="_blank" rel="noreferrer"><code>cameraui/docker</code></a>-Repository enthält einen Host-Helfer, der deine Maschine diagnostiziert und die Treiber installiert, die mehr als einen Paketmanager brauchen:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">curl</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -fsSLO</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> https://raw.githubusercontent.com/cameraui/docker/main/scripts/host/cameraui-host.sh</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">sudo</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> bash</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> cameraui-host.sh</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> check</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">     # read-only: Treiber, Docker-Runtime, Device-Nodes</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">sudo</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> bash</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> cameraui-host.sh</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> nvidia</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">    # NVIDIA Container Toolkit installieren</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">sudo</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> bash</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> cameraui-host.sh</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> hailo</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">     # Hailo-PCIe-Kernel-Treiber bauen + installieren</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">sudo</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> bash</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> cameraui-host.sh</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> coral</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">     # gasket/apex-Treiber installieren (PCIe/M.2-Coral)</span></span></code></pre></div><p><code>check</code> gibt für jede Beschleuniger-Klasse eine ✓/✗-Zeile mit dem passenden Fix aus. Führe es zuerst aus. Es sagt dir, welche der folgenden Abschnitte deine Maschine betreffen.</p><h2 id="aus-dem-container-verifizieren" tabindex="-1">Aus dem Container verifizieren <a class="header-anchor" href="#aus-dem-container-verifizieren" aria-label="Permalink to &quot;Aus dem Container verifizieren&quot;">​</a></h2><p>Bei jedem Start loggt der Container, welche Device-Nodes er tatsächlich sieht:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>[setup] accelerator devices: /dev/dri ✓ · /dev/kfd ✗ · /dev/accel ✗ · /dev/apex_0 ✓ · /dev/hailo0 ✗ · /dev/bus/usb ✗ · nvidia ✗</span></span></code></pre></div><p>Zeigt ein Device hier <code>✗</code>, existiert aber auf dem Host, fehlt es in deiner Compose-<code>devices:</code>-Liste. Die Erkennungs-Plugins loggen zusätzlich, was sie gefunden haben (<code>Available devices: …</code>) und worauf jedes Modell wirklich läuft (<code>Loaded model: … (AUTO -&gt; CPU)</code>), und zeigen es unter <strong>Active Hardware</strong> in ihren Plugin-Einstellungen.</p><h2 id="schnellreferenz" tabindex="-1">Schnellreferenz <a class="header-anchor" href="#schnellreferenz" aria-label="Permalink to &quot;Schnellreferenz&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Hardware</th><th>Beschleunigt</th><th>Host-Treiber</th><th>An den Container durchreichen</th></tr></thead><tbody><tr><td>Intel / AMD GPU</td><td>Video (VA-API/QSV) + OpenCL/Vulkan-Inferenz</td><td>im Kernel (i915/amdgpu)</td><td><code>/dev/dri</code></td></tr><tr><td>NVIDIA GPU</td><td>Video (NVENC/NVDEC) + CUDA</td><td>NVIDIA-Treiber + Container Toolkit</td><td>über die Toolkit-Runtime</td></tr><tr><td>Coral Edge TPU (PCIe/M.2)</td><td>Objekterkennung</td><td>gasket/apex (<code>coral</code>-Befehl)</td><td><code>/dev/apex_0</code></td></tr><tr><td>Coral Edge TPU (USB)</td><td>Objekterkennung</td><td>keiner</td><td><code>/dev/bus/usb</code></td></tr><tr><td>Hailo-8 / Hailo-8L</td><td>Objekterkennung</td><td>hailo_pci + Firmware (<code>hailo</code>-Befehl)</td><td><code>/dev/hailo0</code></td></tr><tr><td>Intel NPU (Core Ultra)</td><td>OpenVINO-Inferenz</td><td><code>intel_vpu</code>-Kernelmodul + Firmware</td><td><code>/dev/accel</code> (intel-Flavor)</td></tr><tr><td>AMD ROCm</td><td>Compute</td><td>ROCm-Stack</td><td><code>/dev/kfd</code> + <code>/dev/dri</code></td></tr></tbody></table><h2 id="intel-amd-gpu" tabindex="-1">Intel / AMD GPU <a class="header-anchor" href="#intel-amd-gpu" aria-label="Permalink to &quot;Intel / AMD GPU&quot;">​</a></h2><p>Der Treiber ist Teil des Linux-Kernels, auf dem Host ist also nichts zu installieren. Nutze den <code>intel</code>- oder <code>amd</code>-Flavor und reiche <code>/dev/dri</code> durch:</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">    devices</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">      - </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">/dev/dri:/dev/dri</span></span></code></pre></div><div class="tip custom-block"><p class="custom-block-title">Ältere Intel-iGPUs (Gen8 bis Gen11, z. B. UHD 630)</p><p>Intels aktuelle OpenCL-Runtime deckt nur noch Gen12 und neuer ab. Der <code>intel</code>-Flavor bündelt deshalb zusätzlich Intels Legacy-Runtime, sodass <strong>OpenVINO- und OpenCL-Inferenz auch auf Gen8- bis Gen11-iGPUs funktioniert</strong> (Broadwell bis Comet Lake). Nur Chips vor Gen8 (Haswell und älter) haben gar keine OpenCL-Runtime: Dort fällt die Inferenz auf die CPU zurück, und ein Coral oder Hailo ist der Weg zu schneller Erkennung. Die Video-Dekodierung ist auf allen ohnehin voll beschleunigt.</p></div><h2 id="nvidia-gpu" tabindex="-1">NVIDIA GPU <a class="header-anchor" href="#nvidia-gpu" aria-label="Permalink to &quot;NVIDIA GPU&quot;">​</a></h2><p>Zwei host-seitige Bausteine: der NVIDIA-Treiber (z. B. <code>sudo ubuntu-drivers install</code>) und das Container Toolkit, das die Treiber-Bibliotheken in Container injiziert. <code>sudo bash cameraui-host.sh nvidia</code> installiert das Toolkit und registriert es bei Docker. Nutze dann das <code>nvidia</code>-Flavor-Override von der <a href="/de/install/docker#hardware-beschleunigung">Docker-Seite</a>. <code>NVIDIA_DRIVER_CAPABILITIES=all</code> aktiviert auch die OpenCL- und Vulkan-Pfade, die die Erkennungs-Plugins nutzen können.</p><h2 id="coral-edge-tpu" tabindex="-1">Coral Edge TPU <a class="header-anchor" href="#coral-edge-tpu" aria-label="Permalink to &quot;Coral Edge TPU&quot;">​</a></h2><p>Die Laufzeit-Bibliothek (<code>libedgetpu</code>) ist bereits in jedem Image-Flavor enthalten. Nur die Host-Seite unterscheidet sich je nach Bauform:</p><ul><li><p><strong>PCIe / M.2.</strong> Braucht den gasket/apex-Kernel-Treiber. <code>sudo bash cameraui-host.sh coral</code> installiert ihn (ein gepflegter Community-Build, da Googles Original-Paket auf Kerneln ≥ 6.13 nicht mehr kompiliert). Danach das Device durchreichen:</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">    devices</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">      - </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">/dev/apex_0:/dev/apex_0</span></span></code></pre></div></li><li><p><strong>USB.</strong> Kein Treiber nötig, nur den USB-Bus durchreichen:</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">    devices</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">      - </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">/dev/bus/usb:/dev/bus/usb</span></span></code></pre></div></li></ul><p>Ohne erreichbaren TPU läuft das Coral-Plugin auf der CPU und sagt das in seinem Log. Prüfe seine <code>Available devices:</code>-Zeile, falls die Erkennung langsam wirkt.</p><h2 id="hailo-8-hailo-8l" tabindex="-1">Hailo-8 / Hailo-8L <a class="header-anchor" href="#hailo-8-hailo-8l" aria-label="Permalink to &quot;Hailo-8 / Hailo-8L&quot;">​</a></h2><p>Der Kernel-Treiber wird auf dem Host aus dem Quellcode gebaut: <code>sudo bash cameraui-host.sh hailo</code> installiert die Build-Abhängigkeiten, kompiliert den Treiber und installiert Firmware und udev-Regeln. <strong>Danach neu starten</strong>, damit die Firmware lädt, dann das Device durchreichen:</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">    devices</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">      - </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">/dev/hailo0:/dev/hailo0</span></span></code></pre></div><p>Die Userspace-Runtime liefert das Hailo-Plugin selbst mit. Sonst ist nichts zu installieren.</p><h2 id="intel-npu-core-ultra" tabindex="-1">Intel NPU (Core Ultra) <a class="header-anchor" href="#intel-npu-core-ultra" aria-label="Permalink to &quot;Intel NPU (Core Ultra)&quot;">​</a></h2><p>Der <strong>intel-Flavor</strong> bündelt den Userspace-Treiber der NPU (Level Zero + Intel NPU UMD und Compiler); andere Flavors nicht. Auf dem Host brauchst du das <code>intel_vpu</code>-Kernelmodul (upstream seit Linux 6.8) und dessen Firmware; falls <code>/dev/accel</code> fehlt, siehe <a href="https://github.com/intel/linux-npu-driver" target="_blank" rel="noreferrer">intel/linux-npu-driver</a>. Dann das Device durchreichen:</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">    devices</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">      - </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">/dev/accel:/dev/accel</span></span></code></pre></div><p>Das OpenVINO-Plugin listet dann <code>NPU</code> in seiner <code>Available devices:</code>-Log-Zeile und in seiner Device-Einstellung.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("de/install/hardware-acceleration.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const hardwareAcceleration = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  hardwareAcceleration as default
};
