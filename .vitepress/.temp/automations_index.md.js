import { resolveComponent, useSSRContext } from "vue";
import { ssrRenderAttrs, ssrRenderStyle, ssrRenderComponent } from "vue/server-renderer";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Building automations","description":"","frontmatter":{"title":"Building automations"},"headers":[],"relativePath":"automations/index.md","filePath":"automations/index.md","lastUpdated":1785427198000}');
const _sfc_main = { name: "automations/index.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  const _component_Shot = resolveComponent("Shot");
  _push(`<div${ssrRenderAttrs(_attrs)}><div style="${ssrRenderStyle({ "display": "none" })}" hidden="true" aria-hidden="true" data-nosnippet>Are you an LLM? You can read better optimized documentation at /automations.md for this page in Markdown format</div><h1 id="building-automations" tabindex="-1">Building automations <a class="header-anchor" href="#building-automations" aria-label="Permalink to &quot;Building automations&quot;">​</a></h1><p>Automations connect what happens at your cameras to actions camera.ui can take. You build them visually: drag <strong>nodes</strong> onto a canvas and connect them into a flow. Automations are an admin feature.</p>`);
  _push(ssrRenderComponent(_component_Shot, {
    src: "/img/automations/editor-canvas.png",
    alt: "Automation editor canvas"
  }, null, _parent));
  _push(`<h2 id="how-a-flow-works" tabindex="-1">How a flow works <a class="header-anchor" href="#how-a-flow-works" aria-label="Permalink to &quot;How a flow works&quot;">​</a></h2><p>A flow runs from left to right: a <strong>trigger</strong> starts it, optional <strong>conditions</strong> decide whether it continues, and <strong>actions</strong> do something. <strong>Utilities</strong> help pass data between steps.</p><p>Open <strong>Automations</strong> from the menu, then create one with the <strong>+</strong> button. A new flow starts empty and disabled, so you can build it before it runs.</p><h2 id="triggers" tabindex="-1">Triggers <a class="header-anchor" href="#triggers" aria-label="Permalink to &quot;Triggers&quot;">​</a></h2><p>A trigger is what starts a flow. You can use more than one.</p><ul><li><strong>Detection event.</strong> Something detected at a camera: motion, a person, vehicle, or animal, an audio event like a doorbell or glass breaking, a recognised face, or a license plate. Filter by object, confidence, and event phase (start, update, end).</li><li><strong>Sensor change.</strong> A sensor&#39;s state changes, for example a contact sensor opening.</li><li><strong>Schedule.</strong> A recurring time, set with a cron expression.</li><li><strong>System event.</strong> Something in camera.ui itself, such as a camera connecting or disconnecting, or a plugin starting or stopping.</li><li><strong>Webhook.</strong> An external service calls a URL to start the flow.</li><li><strong>MQTT message.</strong> A message arrives on an MQTT topic. Set the topic (<code>+</code> and <code>#</code> wildcards work) and choose how to match: any message, an exact payload, or a value at a JSON path like <code>params.switch:0.output</code>. Needs the MQTT connection set up in Settings.</li><li><strong>Geofence.</strong> A user enters or leaves a location you define.</li><li><strong>Manual.</strong> You run the flow yourself with the Run button, useful while building and testing.</li></ul><p><a href="/sensors/virtual">Virtual sensors</a> work here too. You create them on the Sensors page, then set them with a <strong>Control sensor</strong> action and react to them with a <strong>Sensor change</strong> trigger.</p><h2 id="conditions" tabindex="-1">Conditions <a class="header-anchor" href="#conditions" aria-label="Permalink to &quot;Conditions&quot;">​</a></h2><p>Conditions decide whether a flow continues:</p><ul><li><strong>If / else.</strong> Branch on a comparison.</li><li><strong>Switch.</strong> Branch on several possible values.</li><li><strong>Sensor state.</strong> Check one or more sensors, combined with AND or OR. Each value can be a fixed value or a variable from an earlier step.</li><li><strong>Time.</strong> Only continue within a time-of-day range and on chosen days.</li></ul><h2 id="actions" tabindex="-1">Actions <a class="header-anchor" href="#actions" aria-label="Permalink to &quot;Actions&quot;">​</a></h2><p>Actions are what the flow does:</p><ul><li><strong>Send notification.</strong> Push an alert with a title, message, and severity.</li><li><strong>Capture snapshot.</strong> Take a fresh image from a camera.</li><li><strong>Control sensor.</strong> Set an accessory, such as turning on a light or locking a door. Values can be fixed or taken from a variable.</li><li><strong>Camera control.</strong> Change a camera setting, like snoozing detection.</li><li><strong>HTTP request.</strong> Call an external service.</li><li><strong>MQTT publish.</strong> Send a message to an MQTT topic. Set the topic and the payload, and turn on <strong>Retain</strong> if the broker should keep the message for anyone subscribing later. Wildcards aren&#39;t allowed in the topic. Needs the MQTT connection set up in Settings.</li><li><strong>Plugin call.</strong> Run a detection or analysis plugin on an image.</li><li><strong>Set variable</strong> and <strong>delay.</strong> Hold a value, or wait before the next step.</li></ul><h2 id="utilities" tabindex="-1">Utilities <a class="header-anchor" href="#utilities" aria-label="Permalink to &quot;Utilities&quot;">​</a></h2><ul><li><strong>Image input.</strong> Load an image (from a URL, a variable, or an upload) for an action to use.</li><li><strong>Output.</strong> Collect results to view after the flow runs.</li></ul><h2 id="options-and-running" tabindex="-1">Options and running <a class="header-anchor" href="#options-and-running" aria-label="Permalink to &quot;Options and running&quot;">​</a></h2><p>In the toolbar you can name the flow, turn it <strong>Enabled</strong> on or off, and set two behaviours:</p><ul><li><strong>Ignore repeated events</strong>, so a burst of triggers runs the flow once.</li><li><strong>Wait for completion</strong>, so a new trigger waits until the current run finishes.</li></ul><p>Save with the <strong>Save</strong> button. If your flow has a <strong>Manual</strong> trigger, a <strong>Run</strong> button lets you test it. In the list, each automation shows when it last ran, and camera.ui disables one automatically if it points at a camera or sensor you&#39;ve removed, with a notice to fix it.</p><p>To share automations, see <a href="/automations/blueprints">Blueprints &amp; store</a>.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("automations/index.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const index = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  index as default
};
