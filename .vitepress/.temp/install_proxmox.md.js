import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Proxmox","description":"","frontmatter":{"title":"Proxmox"},"headers":[],"relativePath":"install/proxmox.md","filePath":"install/proxmox.md","lastUpdated":1785095507000}');
const _sfc_main = { name: "install/proxmox.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><div style="${ssrRenderStyle({ "display": "none" })}" hidden="true" aria-hidden="true" data-nosnippet>Are you an LLM? You can read better optimized documentation at /install/proxmox.md for this page in Markdown format</div><h1 id="proxmox" tabindex="-1">Proxmox <a class="header-anchor" href="#proxmox" aria-label="Permalink to &quot;Proxmox&quot;">​</a></h1><p>camera.ui runs on Proxmox VE inside an LXC container that hosts Docker. One command on the Proxmox host sets up everything: the container, Docker, and camera.ui.</p><div class="tip custom-block"><p class="custom-block-title">Why an LXC and not a VM?</p><p>An LXC can <strong>share</strong> the iGPU: <code>/dev/dri</code> stays available to the host and other containers at the same time, while a VM with PCIe passthrough monopolizes the GPU. Add the lower overhead and this is the standard pattern for NVRs on Proxmox. A VM is the better choice for NVIDIA cards (see below).</p></div><h2 id="quick-start" tabindex="-1">Quick start <a class="header-anchor" href="#quick-start" aria-label="Permalink to &quot;Quick start&quot;">​</a></h2><p>Run this on the Proxmox host (as root):</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">curl</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -fsSLO</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> https://raw.githubusercontent.com/cameraui/docker/main/proxmox/install-cameraui-lxc.sh</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">bash</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> install-cameraui-lxc.sh</span></span></code></pre></div><p>The script downloads the Ubuntu 24.04 LXC template, creates an <strong>unprivileged</strong> container with nesting (the recommended way to run Docker on Proxmox, since privileged containers can no longer load Docker&#39;s AppArmor profile on current Proxmox releases), installs Docker inside it, deploys camera.ui via Docker Compose and prints the URL of the web UI. Tested with Proxmox VE 9.</p><p>Everything is tunable via environment variables:</p><table tabindex="0"><thead><tr><th>Variable</th><th>Default</th><th>Meaning</th></tr></thead><tbody><tr><td><code>CTID</code></td><td>next free ID</td><td>container ID</td></tr><tr><td><code>CT_HOSTNAME</code></td><td><code>cameraui</code></td><td>container hostname</td></tr><tr><td><code>CORES</code> / <code>RAM_MB</code> / <code>DISK_GB</code></td><td><code>4</code> / <code>4096</code> / <code>16</code></td><td>container resources</td></tr><tr><td><code>BRIDGE</code></td><td><code>vmbr0</code></td><td>network bridge (container gets its IP via DHCP)</td></tr><tr><td><code>STORAGE</code></td><td>auto-detect</td><td>rootfs storage (prefers <code>local-lvm</code>, <code>local-zfs</code>, <code>local</code>, otherwise the first active storage that can hold a container rootfs)</td></tr><tr><td><code>TEMPLATE_STORAGE</code></td><td><code>local</code></td><td>storage the LXC template is downloaded to (needs content type &quot;Container template&quot;)</td></tr><tr><td><code>FLAVOR</code></td><td><code>cpu</code></td><td><code>cpu</code>, <code>intel</code> or <code>amd</code> (picks the <a href="/install/docker#hardware-acceleration">image flavor</a>); <code>nvidia</code> is <a href="#nvidia-in-an-lxc-experimental">experimental</a></td></tr><tr><td><code>GPU_PASSTHROUGH</code></td><td><code>1</code> when flavor ≠ cpu</td><td>pass <code>/dev/dri</code> into the container</td></tr><tr><td><code>TZ</code></td><td>host timezone</td><td>timezone inside the container</td></tr></tbody></table><p>Example for an Intel machine: <code>FLAVOR=intel bash install-cameraui-lxc.sh</code></p><h2 id="getting-a-shell-in-the-container" tabindex="-1">Getting a shell in the container <a class="header-anchor" href="#getting-a-shell-in-the-container" aria-label="Permalink to &quot;Getting a shell in the container&quot;">​</a></h2><p>The script creates the container without a root password, so the Proxmox console and SSH won&#39;t let you in. Use the host shell instead:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pct</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> enter</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> &lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">CTI</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">D</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span></span></code></pre></div><p>That puts you in a root shell inside the container. Single commands don&#39;t need you to enter it at all:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pct</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> exec</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> &lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">CTI</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">D</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> docker</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> logs</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> cameraui</span></span></code></pre></div><p>If you&#39;d rather log in the usual way, set a password once. Console and SSH work from then on:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pct</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> exec</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> &lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">CTI</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">D</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> passwd</span></span></code></pre></div><h2 id="hardware-acceleration" tabindex="-1">Hardware acceleration <a class="header-anchor" href="#hardware-acceleration" aria-label="Permalink to &quot;Hardware acceleration&quot;">​</a></h2><ul><li><strong>Intel / AMD iGPU.</strong> Set <code>FLAVOR=intel</code> (or <code>amd</code>). The script passes the GPU render node into the container via Proxmox&#39;s device passthrough and wires it through to Docker. Nothing else to configure; the details live on the <a href="/install/hardware-acceleration">Hardware acceleration</a> page.</li><li><strong>NVIDIA.</strong> Use a <strong>VM with PCIe passthrough</strong> instead of an LXC. Inside the VM you get a plain Linux machine and follow the standard <a href="/install/docker">Docker setup</a> with the <code>nvidia</code> flavor. An LXC would require keeping the NVIDIA driver on the Proxmox host and the user-space libraries inside the container in permanent version lockstep. A VM avoids that entirely, and Proxmox&#39;s VM passthrough is excellent. The trade-off: the VM monopolizes the card. If other services on the host need the same GPU (Ollama, Jellyfin), see <a href="#nvidia-in-an-lxc-experimental">NVIDIA in an LXC</a> below.</li><li><strong>AI accelerators (Coral, Hailo).</strong> Same pattern as everywhere: install the <a href="/install/hardware-acceleration#check-your-host">host driver</a> on the Proxmox host, then pass the device node (<code>/dev/apex_0</code>, <code>/dev/hailo0</code>) into the container.</li></ul><h3 id="nvidia-in-an-lxc-experimental" tabindex="-1">NVIDIA in an LXC (experimental) <a class="header-anchor" href="#nvidia-in-an-lxc-experimental" aria-label="Permalink to &quot;NVIDIA in an LXC (experimental)&quot;">​</a></h3><p><code>FLAVOR=nvidia</code> shares the card between the container and other services on the host, which a VM cannot do. The catch is a hard version coupling: the NVIDIA user-space libraries inside the container must exactly match the host&#39;s kernel driver, after every driver update.</p><p>It needs a working NVIDIA driver on the Proxmox host first (<code>.run</code> installer with <code>--dkms</code> plus <code>pve-headers</code>; <code>nvidia-smi</code> on the host must work), and the installed driver version must exist on <code>download.nvidia.com</code>. Then:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">FLAVOR</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">nvidia</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> bash</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> install-cameraui-lxc.sh</span></span></code></pre></div><p>The script passes all <code>/dev/nvidia*</code> device nodes into the container, installs the matching user-space driver (<code>--no-kernel-module</code>) plus the NVIDIA Container Toolkit (with <code>no-cgroups=true</code>, because an unprivileged container cannot manage device cgroups), and registers a boot-time sync service that re-installs the matching user space after host driver updates. Verify with:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pct</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> exec</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> &lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">CTI</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">D</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> docker</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> exec</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> cameraui</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> nvidia-smi</span></span></code></pre></div><div class="warning custom-block"><p class="custom-block-title">Experimental</p><p>This path depends on NVIDIA&#39;s installer archive and on the host driver staying healthy; after a host driver update the container needs a reboot to re-sync. If it breaks, the VM route always works. Reports and fixes are welcome in the <a href="https://github.com/cameraui/docker" target="_blank" rel="noreferrer">docker repo</a>.</p></div><h2 id="recordings-on-a-dedicated-disk-or-nas" tabindex="-1">Recordings on a dedicated disk or NAS <a class="header-anchor" href="#recordings-on-a-dedicated-disk-or-nas" aria-label="Permalink to &quot;Recordings on a dedicated disk or NAS&quot;">​</a></h2><p>Recordings default to the container&#39;s own volume. Point them at a bigger disk, a NAS share, or a merged pool by handing that storage into the container and setting the NVR&#39;s storage path to it.</p><p>Unprivileged containers cannot mount NFS/CIFS shares themselves. Mount the share on the <strong>Proxmox host</strong> and hand it into the container as a mount point:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pct</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> set</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> &lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">CTI</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">D</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -mp0</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> /mnt/nas/recordings,mp=/mnt/recordings</span></span></code></pre></div><p>Then set the NVR <strong>Storage Path</strong> to <code>/mnt/recordings</code> (Settings → Recordings → Storage), or see the <a href="/install/docker#storage-for-recordings">storage section</a> of the Docker page.</p><div class="warning custom-block"><p class="custom-block-title">FUSE mounts (mergerfs, rclone) need extra care</p><p>A FUSE mount (such as a <strong>mergerfs</strong> pool merging several drives) does not propagate into an (unprivileged) container the way a normal filesystem does. If the FUSE mount isn&#39;t active on the host <strong>before the container starts</strong>, the container sees the empty mountpoint on the host&#39;s root disk instead of the pool. Recordings then land on the small OS disk and get rotated away within minutes, even though the pool has terabytes free.</p><p>Mount the pool on the host <strong>before</strong> the container starts (e.g. via an fstab entry or a systemd unit ordered <code>Before=pve-container@&lt;CTID&gt;.service</code>), and make sure the mount is shared into the container. Then always <strong>verify from inside the container</strong>.</p></div><h2 id="verify-your-storage-size" tabindex="-1">Verify your storage size <a class="header-anchor" href="#verify-your-storage-size" aria-label="Permalink to &quot;Verify your storage size&quot;">​</a></h2><p>After setting a custom storage path, confirm the container really sees the full volume. This one check catches almost every mount problem:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pct</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> exec</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> &lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">CTI</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">D</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> df</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -h</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> /mnt/recordings</span></span></code></pre></div><p>The reported size must match your real disk or pool. If it shows the small container root size instead, the mount didn&#39;t propagate. Recheck the steps above. The NVR also logs its resolved storage and size on every start, and shows a warning in <strong>Settings → Recordings</strong> when the volume is unexpectedly small.</p><h2 id="verify" tabindex="-1">Verify <a class="header-anchor" href="#verify" aria-label="Permalink to &quot;Verify&quot;">​</a></h2><p>On every start the container logs which devices actually arrived. See <a href="/install/hardware-acceleration#verify-from-the-container">Verify from the container</a>. The camera.ui UI is available at <code>https://&lt;container-ip&gt;:3443</code> (the script prints the IP at the end; the container also shows up with its hostname in your router&#39;s device list).</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("install/proxmox.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const proxmox = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  proxmox as default
};
