import { resolveComponent, useSSRContext } from "vue";
import { ssrRenderAttrs, ssrRenderStyle, ssrRenderComponent } from "vue/server-renderer";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Workers","description":"","frontmatter":{"title":"Workers"},"headers":[],"relativePath":"admin/workers.md","filePath":"admin/workers.md","lastUpdated":1784146484000}');
const _sfc_main = { name: "admin/workers.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  const _component_Shot = resolveComponent("Shot");
  _push(`<div${ssrRenderAttrs(_attrs)}><div style="${ssrRenderStyle({ "display": "none" })}" hidden="true" aria-hidden="true" data-nosnippet>Are you an LLM? You can read better optimized documentation at /admin/workers.md for this page in Markdown format</div><h1 id="workers" tabindex="-1">Workers <a class="header-anchor" href="#workers" aria-label="Permalink to &quot;Workers&quot;">​</a></h1>`);
  _push(ssrRenderComponent(_component_Shot, {
    src: "/img/admin/workers.png",
    alt: "The Workers settings"
  }, null, _parent));
  _push(`<p>A <strong>worker</strong> is a second machine that takes on part of a server&#39;s processing. The main server (the <strong>master</strong>) keeps the UI, the settings and the recordings; workers contribute compute. Manage them in <strong>Workers</strong> (in the menu).</p><p>Typical reasons for a worker:</p><ul><li><strong>Busy cameras.</strong> Decoding and detection for selected cameras move to the worker, freeing the master&#39;s CPU and GPU for the rest.</li><li><strong>Hardware the master lacks.</strong> An entire plugin can run on a worker, for example a detection backend that needs a GPU the master doesn&#39;t have, or a plugin the master&#39;s platform can&#39;t load.</li><li><strong>Growing setups.</strong> When you add cameras, add a worker instead of replacing the server.</li></ul><h2 id="how-workers-work" tabindex="-1">How workers work <a class="header-anchor" href="#how-workers-work" aria-label="Permalink to &quot;How workers work&quot;">​</a></h2><p>A worker is a regular camera.ui installation started in worker mode. It has no UI of its own, holds no cameras and stores no recordings; it connects to the master and waits for work.</p><ul><li><strong>The worker connects outward.</strong> Pairing goes to the master&#39;s HTTPS port, the running connection to the master&#39;s worker port (default <code>7422</code>). Only the master has to be reachable; nothing needs to be opened on the worker machine.</li><li><strong>Paired once, then on its own.</strong> A one-time pairing code is exchanged for the worker&#39;s own credentials. Nothing is shared between workers, each one can be revoked independently, and after the first start the worker reconnects by itself.</li><li><strong>Assignments follow the worker.</strong> Cameras and plugins you <a href="#camera-and-plugin-assignments">assign</a> run on the worker, fall back to the master while it&#39;s offline, and move back when it returns (see <a href="#failover">Failover</a>).</li></ul><p>Master and worker don&#39;t need the same installation type, any mix works: a Docker server with an old laptop running the <a href="#worker-with-the-desktop-app">desktop app</a> as its worker, a desktop-app server with a <a href="#worker-in-docker">Docker worker</a> on a mini PC, a bare-metal Linux server with both. The camera.ui version should match across machines; the <a href="#the-workers-list">workers list</a> warns when it doesn&#39;t.</p><h2 id="enable-workers" tabindex="-1">Enable workers <a class="header-anchor" href="#enable-workers" aria-label="Permalink to &quot;Enable workers&quot;">​</a></h2><p>Turn on <strong>Enable Workers</strong> on the master and set a <strong>Master Address</strong>, the LAN IP or hostname other machines use to reach this server. Workers can&#39;t be enabled without an address. A <strong>Worker Connection Port</strong> (default <code>7422</code>) is also required; changing it later means already-paired workers must be re-paired.</p><h2 id="pair-a-worker" tabindex="-1">Pair a worker <a class="header-anchor" href="#pair-a-worker" aria-label="Permalink to &quot;Pair a worker&quot;">​</a></h2><p>Click <strong>Generate Pairing Code</strong> to create a one-time code (valid for 15 minutes). Along with it you get a ready-made configuration snippet:</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">worker</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  master</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&lt;master address&gt;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  apiPort</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&lt;api port&gt;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  pairingCode</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&lt;code&gt;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  name</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">my-worker</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  capabilities</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    - </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">frameDecoding</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    - </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">pluginHost</span></span></code></pre></div><p>The code can only be used once, and only for the first start: the worker exchanges it for its own credentials and reconnects by itself afterwards. How the snippet reaches the worker depends on the installation, see the next sections.</p><h2 id="set-up-the-worker-machine" tabindex="-1">Set up the worker machine <a class="header-anchor" href="#set-up-the-worker-machine" aria-label="Permalink to &quot;Set up the worker machine&quot;">​</a></h2><h3 id="worker-in-docker" tabindex="-1">Worker in Docker <a class="header-anchor" href="#worker-in-docker" aria-label="Permalink to &quot;Worker in Docker&quot;">​</a></h3><p>A worker runs from the same image as the server, just started in worker mode. Save this as <code>docker-compose.worker.yml</code> on the second machine, with your master&#39;s address and the pairing code from above:</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">name</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">cameraui-worker</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">services</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  cameraui-worker</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">    image</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">ghcr.io/cameraui/camera.ui:latest</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">    container_name</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">cameraui-worker</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">    restart</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">unless-stopped</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">    network_mode</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">host</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">    environment</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">      - </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">TZ=Europe/Berlin</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">      - </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">CAMERA_UI_WORKER=true</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">      - </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">CAMERA_UI_WORKER_MASTER=192.168.1.10</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">      - </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">CAMERA_UI_WORKER_PAIRING_CODE=XXXX-XXXX</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">      - </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">CAMERA_UI_WORKER_NAME=worker-1</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">    volumes</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">      - </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">cameraui-worker-data:/data</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">volumes</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  cameraui-worker-data</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span></code></pre></div><p>Then start it:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">docker</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> compose</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -f</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> docker-compose.worker.yml</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> up</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -d</span></span></code></pre></div><p>The master address takes a hostname or IP, without a scheme. <code>CAMERA_UI_WORKER_API_PORT</code> is only needed if the master&#39;s HTTPS port differs from the default. To pass hardware through for decoding, use the same override files as the main deployment, see <a href="/install/hardware-acceleration">Hardware acceleration</a>.</p><h3 id="worker-with-the-desktop-app" tabindex="-1">Worker with the desktop app <a class="header-anchor" href="#worker-with-the-desktop-app" aria-label="Permalink to &quot;Worker with the desktop app&quot;">​</a></h3><p>An old laptop or a mini PC with the <a href="/install/desktop">desktop app</a> installed can join as a worker too. Choose <strong>Worker</strong> in the mode picker on first start (or later via <strong>Switch mode…</strong> in the tray menu), enter the master&#39;s address and the pairing code, and connect. Instead of the normal interface the app shows a small status window with the connection state. If the address is wrong or the code has expired, the picker reopens and shows what failed.</p><p>Two tray options make this hands-off: <strong>Open At Login</strong> starts the app with the system, minimized to the tray, and <strong>Close to tray</strong> keeps the worker running in the background when the window is closed.<sup class="footnote-ref"><a href="#fn1" id="fnref1">[1]</a></sup> The address takes a hostname or IP, with <code>:port</code> only when the master&#39;s HTTPS port differs from the default.</p><h3 id="worker-on-linux-bare-metal" tabindex="-1">Worker on Linux (bare-metal) <a class="header-anchor" href="#worker-on-linux-bare-metal" aria-label="Permalink to &quot;Worker on Linux (bare-metal)&quot;">​</a></h3><p>Paste the snippet into the worker machine&#39;s <code>config.yml</code>, then start it with <code>cameraui --worker run</code>. To keep the machine a worker across reboots, install it as a service instead: <code>cameraui --worker install</code> (the flag is stored with the service, see <a href="/install/linux">Linux (bare-metal)</a>).</p><h3 id="environment-variables" tabindex="-1">Environment variables <a class="header-anchor" href="#environment-variables" aria-label="Permalink to &quot;Environment variables&quot;">​</a></h3><p>You can pass the same values as environment variables instead, which is usually easier in a container. <code>CAMERA_UI_WORKER=true</code> starts worker mode without the <code>--worker</code> option, and <code>CAMERA_UI_WORKER_MASTER</code>, <code>CAMERA_UI_WORKER_API_PORT</code>, <code>CAMERA_UI_WORKER_PAIRING_CODE</code>, <code>CAMERA_UI_WORKER_NAME</code> and <code>CAMERA_UI_WORKER_CAPABILITIES</code> (comma-separated) cover the rest of the snippet. No <code>config.yml</code> is needed then, and if you set both, the environment variable wins.</p><p>Leave the capabilities unset and the worker offers everything, which is what most setups want; the master still only assigns what you give it. Set them to restrict a worker to a single job, for example only hosting plugins.</p><h2 id="the-workers-list" tabindex="-1">The workers list <a class="header-anchor" href="#the-workers-list" aria-label="Permalink to &quot;The workers list&quot;">​</a></h2><p>Once paired, a worker appears in the <strong>Workers</strong> list showing its online/offline status, platform (OS/architecture), process ID, version, and live CPU and memory use. A warning is shown if a worker&#39;s version differs from the master&#39;s.</p><h2 id="camera-and-plugin-assignments" tabindex="-1">Camera and plugin assignments <a class="header-anchor" href="#camera-and-plugin-assignments" aria-label="Permalink to &quot;Camera and plugin assignments&quot;">​</a></h2><p>Under <strong>Camera Assignments</strong>, choose which camera each worker decodes and detects on; cameras left as <strong>Local</strong> stay on the main server.</p><p>Under <strong>Plugin Assignments</strong>, an entire plugin can run on a worker instead of the main server, for example a detection backend that needs hardware the main server lacks. Only workers whose platform is compatible with the plugin are offered. The worker installs and runs the plugin; it&#39;s still configured normally in the UI.</p><h2 id="failover" tabindex="-1">Failover <a class="header-anchor" href="#failover" aria-label="Permalink to &quot;Failover&quot;">​</a></h2><p>Workers send a heartbeat every 5 seconds and are considered offline after 15 seconds without one. If a worker disconnects, its assigned cameras and plugins fall back to the main server automatically (a plugin only falls back if the main server&#39;s platform can run it). When the worker reconnects, its cameras and plugins are automatically re-homed to it.</p><p>Removing a worker revokes its credentials and drops the connection; its assigned cameras and plugins fall back to the main server.</p><hr class="footnotes-sep"><section class="footnotes"><ol class="footnotes-list"><li id="fn1" class="footnote-item"><p>Open At Login is offered on Windows and macOS. Close to tray is offered on Windows and Linux; on macOS, closing the window leaves the app running anyway. <a href="#fnref1" class="footnote-backref">↩︎</a></p></li></ol></section></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("admin/workers.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const workers = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  workers as default
};
