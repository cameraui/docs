import { resolveComponent, useSSRContext } from "vue";
import { ssrRenderAttrs, ssrRenderStyle, ssrRenderComponent } from "vue/server-renderer";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Home Assistant","description":"","frontmatter":{"title":"Home Assistant"},"headers":[],"relativePath":"home-assistant/index.md","filePath":"home-assistant/index.md","lastUpdated":1784660752000}');
const _sfc_main = { name: "home-assistant/index.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  const _component_Shot = resolveComponent("Shot");
  _push(`<div${ssrRenderAttrs(_attrs)}><div style="${ssrRenderStyle({ "display": "none" })}" hidden="true" aria-hidden="true" data-nosnippet>Are you an LLM? You can read better optimized documentation at /home-assistant.md for this page in Markdown format</div><h1 id="home-assistant" tabindex="-1">Home Assistant <a class="header-anchor" href="#home-assistant" aria-label="Permalink to &quot;Home Assistant&quot;">​</a></h1><p>camera.ui works with Home Assistant in two ways that have nothing to do with each other. You can <strong>run camera.ui inside Home Assistant</strong> with the app, and you can <strong>connect an existing camera.ui to Home Assistant</strong> with the integration. They are separate jobs, so you can do either or both: the app runs the server, the integration surfaces its cameras and sensors as HA entities.</p>`);
  _push(ssrRenderComponent(_component_Shot, {
    src: "/img/home-assistant/devices.png",
    alt: "camera.ui devices and entities in Home Assistant"
  }, null, _parent));
  _push(`<h2 id="running-vs-connecting" tabindex="-1">Running vs connecting <a class="header-anchor" href="#running-vs-connecting" aria-label="Permalink to &quot;Running vs connecting&quot;">​</a></h2><ul><li><strong>The app runs camera.ui.</strong> It installs the camera.ui server as a Home Assistant OS app, so your cameras, recordings, and detection live on the same box as HA. This is a way to host camera.ui, nothing more.</li><li><strong>The integration connects camera.ui.</strong> It talks to a camera.ui server (the app, or a Docker/desktop install anywhere on your network) and creates one HA device per camera with live streams, sensors, and PTZ. This is how entities show up in HA.</li></ul><p>If you run the app, you still add the integration to get entities into Home Assistant. If your camera.ui already runs elsewhere, you only need the integration.</p><h2 id="getting-entities-in-pick-one-transport" tabindex="-1">Getting entities in: pick one transport <a class="header-anchor" href="#getting-entities-in-pick-one-transport" aria-label="Permalink to &quot;Getting entities in: pick one transport&quot;">​</a></h2><p>There are two independent ways to get camera.ui cameras and sensors into Home Assistant as entities:</p><ol><li><strong>The integration</strong> (recommended). A custom component that pushes entities to HA over a local connection. No broker needed.</li><li><strong>MQTT discovery</strong>. An opt-in on the camera.ui server&#39;s MQTT bridge that publishes Home Assistant discovery configs to your broker.</li></ol><p>These two do <strong>not</strong> merge. Home Assistant treats them as unrelated, so running both leaves you with two copies of every camera: two devices, two motion sensors, two of everything. Pick one.</p><div class="danger custom-block"><p class="custom-block-title">Do not run both</p><p>The integration and MQTT discovery duplicate each other. Pick one. Use the integration unless you specifically want to route camera.ui through an existing MQTT broker.</p></div><h2 id="the-integration" tabindex="-1">The integration <a class="header-anchor" href="#the-integration" aria-label="Permalink to &quot;The integration&quot;">​</a></h2><p>Recommended for most setups. It connects straight to camera.ui with no MQTT broker involved. Live camera changes arrive by push, and it re-checks the camera list once a minute as a backstop.</p><p>You get, per camera:</p><ul><li><strong>A live camera entity</strong> with streaming. Snapshots and the live source (high, mid, or low RTSP) come straight from camera.ui.</li><li><strong>Motion and object-detection sensors.</strong> Object sensors cover the labels person, vehicle, animal, and package.</li><li><strong>Sensors</strong> across the <code>binary_sensor</code>, <code>sensor</code>, <code>switch</code>, <code>light</code>, <code>siren</code>, <code>lock</code>, <code>cover</code>, and <code>alarm_control_panel</code> domains. Switches, lights, sirens, locks, covers, and alarm panels are controllable from Home Assistant; contact and measurement sensors are read-only.</li><li><strong>A PTZ service</strong>, <code>cameraui.ptz</code>, for continuous, stop, move, absolute, preset, and home actions.</li></ul><p>On top of that, the integration registers the <code>cameraui-card</code> Lovelace resource, embeds the full camera.ui web UI as a sidebar panel, and fires a <code>cameraui_event</code> on the HA event bus for every detection, with device triggers wired up for the automation editor. Setup can start from zeroconf: camera.ui advertises itself over mDNS, so host and port are pre-filled and you only enter an access token.</p><p>See <strong><a href="/home-assistant/integration">Integration</a></strong> for the full setup.</p><h2 id="mqtt-discovery" tabindex="-1">MQTT discovery <a class="header-anchor" href="#mqtt-discovery" aria-label="Permalink to &quot;MQTT discovery&quot;">​</a></h2><p>Use this only if you already run an MQTT broker and want camera.ui to speak through it. Its one real advantage is that it needs no custom component and no HACS: the camera.ui server publishes retained discovery configs, and Home Assistant&#39;s built-in MQTT integration picks them up.</p><p>You turn it on in the camera.ui server&#39;s MQTT settings (enable HA discovery). You get status, motion, and object binary sensors, a snapshot camera image, and the same controllable sensor domains as the integration. What you do not get: a live stream (snapshot only), the PTZ service, the sidebar panel, the auto-registered card, or the <code>cameraui_event</code> bus events. MQTT does add one thing the integration handles differently: a dedicated per-camera connectivity sensor.</p><p>MQTT discovery is documented with the rest of the broker settings on the <a href="/admin/mqtt">MQTT admin page</a>.</p><h2 id="integration-vs-mqtt-discovery" tabindex="-1">Integration vs MQTT discovery <a class="header-anchor" href="#integration-vs-mqtt-discovery" aria-label="Permalink to &quot;Integration vs MQTT discovery&quot;">​</a></h2><table tabindex="0"><thead><tr><th></th><th>Integration</th><th>MQTT discovery</th></tr></thead><tbody><tr><td>Needs an MQTT broker</td><td>No</td><td>Yes</td></tr><tr><td>Custom component / HACS</td><td>Required</td><td>Not needed</td></tr><tr><td>Live camera stream</td><td>Yes</td><td>Snapshot only</td></tr><tr><td>Motion &amp; object sensors</td><td>Yes</td><td>Yes</td></tr><tr><td>Controllable sensors</td><td>Yes</td><td>Yes</td></tr><tr><td>PTZ service</td><td>Yes</td><td>No</td></tr><tr><td>Per-camera connectivity sensor</td><td>No separate one</td><td>Yes</td></tr><tr><td>Embedded web UI panel</td><td>Yes</td><td>No</td></tr><tr><td>Lovelace card auto-registered</td><td>Yes</td><td>No</td></tr><tr><td><code>cameraui_event</code> + device triggers</td><td>Yes</td><td>No</td></tr><tr><td>Discovery</td><td>zeroconf (mDNS)</td><td>over MQTT</td></tr></tbody></table><h2 id="the-lovelace-card" tabindex="-1">The Lovelace card <a class="header-anchor" href="#the-lovelace-card" aria-label="Permalink to &quot;The Lovelace card&quot;">​</a></h2><p>The <code>cameraui-card</code> shows a camera&#39;s live stream with a PTZ overlay on your dashboards. With the integration it is registered for you automatically; you just add it to a view.</p><p>See <strong><a href="/home-assistant/card">Lovelace card</a></strong> for how to place and configure it.</p><h2 id="next-steps" tabindex="-1">Next steps <a class="header-anchor" href="#next-steps" aria-label="Permalink to &quot;Next steps&quot;">​</a></h2><ul><li><strong><a href="/home-assistant/app">App</a></strong> — run the camera.ui server inside Home Assistant OS.</li><li><strong><a href="/home-assistant/integration">Integration</a></strong> — connect a camera.ui server and get cameras, sensors, and PTZ as HA entities.</li><li><strong><a href="/home-assistant/card">Lovelace card</a></strong> — put a camera with PTZ on your dashboard.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("home-assistant/index.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const index = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  index as default
};
