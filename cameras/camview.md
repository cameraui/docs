---
title: Camview
---

# Camview

Camview shows several cameras at once in a grid. You can build custom layouts, arrange them how you like, and control every camera in a layout together.

<Shot src="/img/cameras/camview-grid.png" alt="Camview multi-camera grid" />

## Create a layout

1. Open **Camview** and select **New View**.
2. Give the view a name and pick a grid size, from a single camera up to 26.
3. Select **Next**, then fill the slots. On a wide screen, drag cameras from the list on the right into the slots. On a narrow screen, tap the plus in a slot and pick a camera from the list.
4. Save the view.

You can create several views and switch between them from the view selector at the top.

## Arrange the grid

Turn on **Rearrange** to drag tiles around. On a computer you can also resize a tile by its bottom-right corner, and two helpers make this quick:

- **Auto-arrange** packs the tiles together with no gaps.
- **Fit aspect ratios** sizes each tile to its camera's configured aspect ratio.

Select **Done** when you're finished. On phones the cameras sit in one column, so Rearrange only changes their order, and resizing and the two helpers aren't available there. A view itself is saved to your account. The arrangement is kept in the browser you made it in, and phones and computers keep their own, so a grid tuned for your desktop stays tidy on mobile. **Edit** and **Delete** for a view are in the top bar.

## Control all cameras at once

A floating control in the corner acts on every camera in the view together:

- Fullscreen the whole grid.
- Mute or unmute all.
- Two-way talk to all cameras at once.
- Switch the streaming mode for all.
- Set the activity mode for all (Always On, Standby, Activity).
- Change the resolution for all.

## Focus one camera

Click a tile to expand it to the full view, click it again to go back to the grid. Views with a single camera stay as they are.

## Playback across the grid

Open the timeline to scrub through recordings for the whole view at the same time, with playback kept in sync across every camera. See [Timeline & playback](/recording/timeline-playback).

## Performance

Camview streams each camera at its **low resolution** so a full grid stays smooth, which matters most on phones and tablets. A single view holds up to **26** cameras. Add a low-resolution [source](/cameras/add-camera#sources-and-roles) to your cameras for the best results here.
